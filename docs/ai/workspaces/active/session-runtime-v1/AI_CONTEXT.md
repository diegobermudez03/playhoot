Process: Initiative Implementation Planning (`session-runtime-v1`), between slices. Slice 1's Feature Development sub-process (WORK-0001) reached DONE on 2026-09-18 and closed; Slice 2's Feature Development sub-process (WORK-0003) opened 2026-09-19 as DRAFT, awaiting human review toward READY.
Topic: Session Runtime lifecycle/runtime design
Current stage: Architecture: CLOSED / HUMAN-APPROVED. **Slice 1 - Session Lobby Foundation: DONE (2026-09-18)**; the isolated `game/game` -> `game/management` rename (WORK-0002, not a slice) also reached DONE (2026-09-18). **Slice 2 - Start + First RuntimeTurn: materialized as `docs/work/active/WORK-0003-session-start-first-runtimeturn.md`, Status DRAFT (2026-09-19).** This checkpoint reconciled prior tracking (confirmed clean: no stale `game/game` current-state references remain outside the historically-exempt set WORK-0002 itself already accounted for), reevaluated Slice 2 against the actual post-Slice-1 codebase and the fully-implemented Manager/sessionlock/idempotency pattern, and drafted WORK-0003 with a concrete Approved Design (a new shared `game/session/internal/runtimeturn` executor package; the fatal pre-first-Turn path; Start's outcome/idempotency shape). WORK-0003 carries four explicit Blockers pending human confirmation (see its Blockers section and this workspace's `HUMAN_REVIEW.md`) - it is intentionally left DRAFT, not advanced to READY, per this checkpoint's explicit scope limit (only a human may authorize DRAFT -> READY).
Current execution surface: CODEBASE AGENT
Parent process: this workspace itself (`session-runtime-v1` Initiative Implementation Planning); Slice 1's Feature Development sub-process is closed. Slice 2's Feature Development sub-process (WORK-0003) is now open, DRAFT.
Related durable artifacts: `ARCHITECTURE.md`, `game/README.md`, `identity/README.md`, `docs/ai/KNOWLEDGE_MAP.md`, `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md`, `docs/product/IDEAS.md`, `game/docs/decisions/GAME-ADR-0001` through `GAME-ADR-0022` (see `game/docs/decisions/INDEX.md`; next Game ADR is `GAME-ADR-0023`), `docs/decisions/architecture/ADR-0005-cross-domain-public-entity-references.md`, `identity/docs/decisions/IDENTITY-ADR-0001-identity-user-public-identity-boundary.md`, `game/language/v1/program/README.md`, `game/language/v1/engine/README.md`, `game/language/v1/engine/LOGICAL_CONTRACT.md`, `docs/engineering/standards/cross-domain-reference-naming.md`, `repositories.md`, `error-handling.md`, `data-integrity.md`, `testing.md`, `domain-logic-placement.md`, `function-signatures.md`, `idempotency.md`, `docs/ai/workspaces/active/session-runtime-v1/PLAN.md` (initiative decomposition), `docs/work/completed/WORK-0001-session-lobby-foundation.md` (Slice 1 WORK, DONE), `docs/work/completed/WORK-0002-rename-game-management-package.md` (rename, DONE), `docs/work/active/WORK-0003-session-start-first-runtimeturn.md` (Slice 2 WORK, DRAFT)
Blocked by: human review/confirmation of WORK-0003's four Blockers (shared `runtimeturn` package placement; fatal-path RUNTIME_STATE_INVALID-vs-RUNTIME_EXECUTION_FAILED classification, including Start-chain signal rejection; replaying a fatal `StartOutcomeRuntimeInitFailed` via idempotency; `players` roster ordering) before WORK-0003 can move DRAFT -> READY.
Next action: the human reviews `HUMAN_REVIEW.md`'s checkpoint and WORK-0003's Blockers, and either approves them (enabling DRAFT -> READY) or directs changes. No Slice 2 implementation may begin until WORK-0003 is READY.
Last durable checkpoint (implementation pass, 2026-09-08): implemented CreateSession/JoinSession/LeaveSession under `game/session/usecases/{createsession,joinsession,leavesession}/`, a new Game Management pinned-definition read capability `game/game/usecases/getgamedefinition/`, shared internal mechanics (`game/session/internal/{sessionlock,idempotency,actors,pgerrs,testfixtures}`), and new migrations replacing the old scaffolding schema with `sessions`/`session_actors`/`session_participants`/`join_codes`/`session_requests` (`game/session/internal/storage/migrations/2026090800000{0..5}_*.go`); removed the non-compiling `game/session/workflows/sessionlifecycle/` tree and the unused `game/session/api.go`. Added `github.com/google/uuid` as a new dependency for UUID generation (a local implementation decision, not previously available anywhere in the repo). Updated `game/CURRENT_STATE.md`, `game/docs/DATA_MODEL.md`, and `game/docs/FLOWS.md` to reflect the new implemented behavior. **Known limitation**: this sandbox has no reachable Postgres/Docker engine, so repository integration tests (including the required concurrency tests) could not actually be executed here - they were written and compile-checked (`go build ./...`, `go vet ./...`) but need a real run (`docker compose up -d postgres` + `TEST_DATABASE_*` env vars + `go test ./game/...`) before independent review treats them as passing. Service-level tests (mocked collaborators) do run in this sandbox and pass. See the full IMPLEMENTATION REPORT delivered at the end of this session for details. Prior checkpoint's corrections (still valid, preserved below): (1) **pinned Game Definition immutability** - `sessions.game_definition_uuid` is resolved exactly once at Create and never re-resolved against "current version" by any later operation (Join here; Start/RuntimeTurn later) - a new "Pinned Game Definition Is Immutable For The Session" subsection and a worked example (Session pins V1; author publishes V2; Join still enforces V1's `players.max`) were added; (2) **a minimum new Game Management read capability** is now required - loading an immutable Definition by its own Definition/Version UUID (not by Game UUID + "current"), consumed by Join and reusable by Slice 2's Start; the existing `getgame.GetPlayableGameWithCurrentVersion` remains Create-only; (3) **idempotency namespace corrected** to `(user_uuid, operation, idempotency_key)` (not `(operation, idempotency_key)`) throughout the persistence model, constraints, and acceptance criteria, so two different users can never collide on the same opaque key; (4) **per-operation idempotency payload fields spelled out explicitly** (CREATE: GameUUID + host UserUUID; JOIN: JoinCode+UserUUID+display name; LEAVE: SessionUUID+UserUUID); (5) **concurrent-Create correctness** - a new "Concurrent Create Correctness" subsection requires claiming the idempotency identity (inserting the `session_requests` row) before any Session-creation effect, using the identity's unique constraint to serialize concurrent claims, with re-read-and-replay on conflict; (6) **completed outcomes vs. transient failures** - a new `session_requests.status` field and subsection make clear a transient infrastructure failure/rollback before commit must never leave behind a permanently replayable "completed" outcome; (7) **Session/HostActor insertion-cycle** - a new "Host/SessionActor Creation Cycle" subsection specifies the approved BEGIN/insert-Session-with-null-host/insert-actor/update-host_actor_id/COMMIT sequence, with `host_actor_id` nullable at storage level only and a strict non-null commit invariant; (8) **new/updated acceptance criteria** added for: pinned-definition enforcement across a version change, cross-domain pinned-vs-current read correctness, idempotency-namespace non-collision across users, concurrent-Create producing exactly one Session, conflicting-payload rejection, and transient-failure non-replayability. `PLAN.md` and this file were synchronized: WORK-0001 is now referenced as READY everywhere it's mentioned, and the human-approved 10-slice sequence (unchanged from the prior checkpoint) is preserved. No production Go code, tests, or SQL migrations were written or changed in this handoff.
Last durable checkpoint (drift-reconciliation pass, 2026-09-11): Reconciled the active Session Runtime implementation/work artifacts against the already-accepted GAME-ADR-0001 (Game Management/Session Runtime capability persistence and transaction boundary). **Implementation drift found: none.** Verified locally (not merely assumed) that `game/session/workflows/sessionlifecycle/` no longer exists, `game/session/usecases/{createsession,joinsession,leavesession}/` and their `repo.go`/`service.go` files were fully read, and: CreateSession resolves/compiles the Game definition via `getgame.GetPlayableGameWithCurrentVersion` (a plain in-process call against Game Management's own `*gorm.DB`) before ever calling its own `c.repo.createSession`, which opens Session Runtime's own transaction via `utils.RunInDBTransaction`; JoinSession resolves the pinned `game_definition_uuid` via an unlocked read (`resolveActiveSessionForJoinCode`) and then calls the narrow `getgamedefinition`-shaped `gameDefinitionResolver.GetGameDefinition` capability, both before opening its own mutation transaction/row lock (`sessionlock.LockByID` only happens inside `joinSession`'s own `RunInDBTransaction` call, called after the Management read returns); no Session `*gorm.DB`/transaction handle is ever passed into a Game Management call in either use case; no Session repository code queries Game Management tables; Game Management's repository is never treated as Session's contract (`gameDefinitionResolver` is a narrow interface Session itself declares). No RPC/events/distributed machinery exists anywhere in this path - both calls are plain in-process Go calls. `go build ./...` and `go vet ./...` are clean repository-wide; `go test ./game/session/... ./game/game/...` (forced with `-count=1`) shows all service-level mocked tests passing and all repository-integration/concurrency tests self-reporting `SKIP` (no reachable Postgres/Docker engine in this sandbox - consistent with the known limitation already recorded in the 2026-09-08 checkpoint below; not new information). **Implementation changes made: none** - no production Go code, tests, or migrations were touched, per this task's explicit scope. **Stale documentation/process state corrected** (all pre-existing text describing pre-Slice-1 scaffolding as if still current, or terminology contradicting GAME-ADR-0001): (1) `PLAN.md`'s "Actual Implementation Drift Found" section - added a HISTORICAL status note and inline `[HISTORICAL - RESOLVED BY SLICE 1]`/`[PARTIALLY RESOLVED BY SLICE 1]` labels on the now-stale scaffolding/no-DB-locking bullets, without deleting the historical text itself; (2) `WORK-0001-session-lobby-foundation.md` - added an "Implementation Status Note" summarizing current reality up front, labeled its pre-implementation "Context" evidence as historical, fixed the internally self-contradictory "Completion Record" (previously still said "Status is READY (implementation has not started)" despite the header already saying IMPLEMENTING), annotated "Verification" with what has actually been run so far in this sandbox, annotated "Data / Migration Impact" to point at the migrations that already implement the proposed approach, and annotated "Documentation Impact" to note `game/CURRENT_STATE.md`/`game/docs/DATA_MODEL.md` were already synchronized ahead of formal closure; (3) `game/docs/DATA_MODEL.md` and `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` - both mislabeled `sessions.game_definition_uuid -> game_definitions.uuid` as a "cross-domain reference, different domain"; split into a new "cross-capability reference within Game" category (citing GAME-ADR-0001) versus the genuinely cross-domain `user_uuid -> Identity.User` references, in both files' two affected sections each. **WORK-0001 status: unchanged, IMPLEMENTING** (not modified by this task per its explicit instruction; independent review has still not been performed, and the mandatory standard-compliance migration below is still outstanding - neither closure precondition in `docs/work/README.md`/`docs/ai/protocols/IMPLEMENTATION_REVIEW.md` is met). **Session Runtime V1 is NOT yet cleanly ready to proceed to Slice 2**: independent review of WORK-0001 has not happened, and the mandatory standard-compliance migration (move Create/Join/Leave into a discoverable workflow package; remove/refactor `game/session/internal/actors`) recorded above under "Mandatory Standard-Compliance Migration" is still outstanding - both remain open gates, unchanged by this reconciliation pass. No redesign was performed and Slice 2 was not started, per this task's explicit scope.
Last durable checkpoint (standard-compliance migration pass, 2026-09-11): Performed the mandatory standard-compliance migration recorded below - see "Mandatory Standard-Compliance Migration" (now marked COMPLETED) and `docs/work/active/WORK-0001-session-lobby-foundation.md` -> "Standard-Compliance Migration Record" for the full detail. Summary: `git mv`'d `game/session/usecases/{createsession,joinsession,leavesession}/` to `game/session/workflows/sessionlifecycle/{createsession,joinsession,leavesession}/` (package names unchanged; only import paths and generated-mock source-comment headers updated accordingly); deleted `game/session/internal/actors/actors.go`; inlined its Find/Create/FindParticipant/CreateParticipant/ReactivateParticipant/RefreshActiveDisplayName/DeactivateParticipant/CountActive behavior as unexported functions directly inside `joinsession/repo.go` (all of them) and `leavesession/repo.go` (only the read-side subset Leave actually uses: findActor/findParticipant/deactivateParticipant) - same SQL, same semantics, no behavior change; left `sessionlock`/`idempotency`/`pgerrs` untouched as legitimate shared mechanisms. Updated documentation to match: `game/CURRENT_STATE.md` and `game/docs/FLOWS.md` evidence paths, and `WORK-0001`'s Implementation Freedom note about package layout (previously stale, recommending the now-superseded `usecases/` layout). Did not touch `game/session/session.go` (domain vocabulary, correctly placed already), `game/session/internal/testfixtures` (test-only seeding helpers, not a production horizontal API), or any migration/storage files. Verification: `go build ./...` and `go vet ./...` clean repository-wide; `go test -count=1 ./game/...` - all service-level tests pass, all repository-integration/concurrency tests still self-report `SKIP` (same known sandbox limitation - no reachable Postgres/Docker engine here - not a new gap introduced by this change, and not claimed as verified). No Slice-2 functionality was added; no GAME-ADR-0001 boundary was touched; WORK-0001 `Status:` was left at IMPLEMENTING (not modified) since the remaining two gates (real-Postgres verification, independent review) are still open.
Last durable checkpoint (engineering-standard/design-revision handoff, 2026-09-16): Documentation-only handoff; **no Go source, tests, or migrations were touched**. Persisted refined reusable engineering standards: created `docs/engineering/standards/function-signatures.md` (explicit-parameter method contracts, no argument-count threshold, semantic primitive types, result-struct guidance) and `docs/engineering/standards/idempotency.md` (natural vs. request/command idempotency, required token, replay/conflict/new-command token semantics, idempotency responsibility ownership, claim-as-concurrency-mechanism, completed-outcome-vs-transient-failure); refined `domain-logic-placement.md` (superseded the prior "No Domain-Wide God Service" wording with "Workflow Controller vs Domain-Wide God Service" - a single cohesive workflow `Manager` exposing genuine lifecycle steps is now the preferred shape, not merely tolerated; added "Preferred Workflow Package Shape" - one package, one `Manager`, steps split by file, not by sibling package; added "Responsibility Categories: Business/Lifecycle Policy, Data Integrity, Persistence Mechanics"); refined `repositories.md` (added "Transaction Ownership" - workflow decides transaction scope, infrastructure performs BEGIN/COMMIT/ROLLBACK, a business rejection can still commit an intended durable outcome; added "Repository Naming" - persistence-oriented names, not business-command names); updated `docs/engineering/standards/INDEX.md` accordingly. Created `game/docs/decisions/GAME-ADR-0021-session-lobby-command-idempotency-token-semantics.md` (ACCEPTED, refines - does not supersede - GAME-ADR-0004's Join wording: a differently-tokened Join while already an active Participant is a new command evaluated against current state, rejected `AlreadyJoined`-equivalent, never silently replayed/succeeded merely because the desired state already holds) and updated `game/docs/decisions/INDEX.md` (next Game ADR is now `GAME-ADR-0022`) and `game/README.md`'s Join paragraph to the token-aware wording. Revised `docs/work/active/WORK-0001-session-lobby-foundation.md` against these clarified rules (returned to DRAFT for the revision, then re-approved READY per the human's explicit approval of this handoff's decisions) - see its "2026-09-16 Design Revision" section for the full list of material design changes (one `Manager` workflow controller replacing three sibling packages, explicit-parameter methods, explicit transaction ownership, persistence-oriented repository naming, expiration/admission ownership moved to the Manager, token-aware Join idempotency) and its correspondingly expanded Acceptance Criteria. The pre-existing Create/Join/Leave implementation was deliberately left untouched and now needs a further implementation pass to comply with this revised design before Slice 2 begins - see "Current Implementation Facts And Drift To Carry Forward" below. `PLAN.md` was annotated (not advanced to Slice 2) to record this further revision.
Last durable checkpoint (design-revision implementation pass, 2026-09-17): Implemented WORK-0001's 2026-09-16 revised Approved Design. Replaced `game/session/workflows/sessionlifecycle/{createsession,joinsession,leavesession}/` with one `game/session/workflows/sessionlifecycle` package: `manager.go` (`Manager` struct, `New`, a `transactor` interface/`dbTransactor` implementation wrapping `utils.RunInDBTransaction`), `types.go` (semantic parameter/result types - `GameUUID`/`UserUUID`/`SessionUUID`/`JoinCode`/`DisplayName`/`IdempotencyKey`/`CreatedSession`/`JoinResult`/`LeaveResult`), `step_create.go`/`step_join.go`/`step_leave.go` (`Manager.Create`/`.Join`/`.Leave`, each with its own narrow `createRepoAPI`/`joinRepoAPI`/`leaveRepoAPI` persistence contract and its own Game Management reader interface where applicable), `expiration.go` (shared `materializeExpirationIfDue` - Manager-owned expiration policy, called by both Join and Leave), `idempotency.go` (per-operation request-payload structs and `interpretExisting*Claim` replay/conflict/rejection-outcome interpretation - Manager-owned idempotency policy), and `internal/repo/` (`repo.go`/`session.go`/`actor.go`/`participant.go`/`join_code.go`/`request.go` - persistence-oriented method names per `repositories.md`, e.g. `LockSessionByID`/`SetSessionTerminal`/`RevokeActiveJoinCode`/`CreateSessionWithHost`/`CreateJoinCode`/`FindActor`/`CreateActor`/`FindParticipant`/`CountActiveParticipants`/`CreateParticipant`/`ActivateParticipant`/`DeactivateParticipant`/`ClaimSessionRequest`/`CompleteSessionRequest`). `game/session/internal/sessionlock` was narrowed to `LockByID`/`LockByUUID` only - its `MaterializeExpirationIfDue` (which decided expiration policy from inside the lock primitive) was removed; that decision now lives in `expiration.go`. Added `session.ErrIdempotencyKeyRequired` and `session.ErrAlreadyJoined` to `game/session/session.go`. **Local implementation decision (not a material/architecture change, but worth recording)**: introduced a `transactor` interface (`RunInTransaction(ctx, func(ctx, tx) error) error`) rather than having the Manager call `utils.RunInDBTransaction` directly, specifically so Manager.Create/.Join/.Leave - which now own all the business/lifecycle policy previously buried in the old repo methods - are unit-testable with mocked collaborators (per `testing.md`'s convention that service/use-case layers get mocked-collaborator tests), and so a business rejection discovered after an idempotency claim already succeeded can commit that claim's completion (with the rejection as its outcome) instead of being rolled back with it. This also **fixes a genuine, previously-unverified correctness bug**: the pre-revision code returned a business error (e.g. `ErrLobbyExpired`) directly from `RunInDBTransaction`'s callback after already performing the lazy-expiration `UPDATE` earlier in the same callback, which caused the transaction to roll back and silently discard that just-performed materialization - exactly the failure mode WORK-0001's Transaction Ownership section was revised to prevent. The fix: a step's callback returns a real Go `error` (causing rollback) only for genuine infrastructure failures; a deterministic business rejection is instead carried out through a closure-captured `bizErr` variable while the callback itself returns `nil` (committing), so an already-performed durable outcome survives. Wrote/updated tests: `internal/repo/{session,actor,participant,join_code,request}_test.go` (real-Postgres repository tests per narrow method), `manager_{create,join,leave}_test.go` (mocked-collaborator Manager policy tests - idempotency replay/conflict/in-flight, `AlreadyJoined`/lobby-full rejection with committed idempotency-completion outcome, lazy-expiration materialization-then-rejection, missing-token rejection), `manager_{create,join,leave}_integration_test.go` and `manager_concurrency_test.go` (real-Postgres end-to-end Manager tests, including the required concurrent-Create/concurrent-Join/Join-racing-Leave/expiration-race proofs), `testutil_test.go` (shared compilable/uncompilable definitions and stub Game Management readers). Updated `game/CURRENT_STATE.md` and `game/docs/FLOWS.md` to describe the new Manager-based structure; updated the stale `testfixtures` package doc comment. Verification: `go build ./...` and `go vet ./...` clean repository-wide; `gofmt -l` clean for new/changed files; `go test ./game/... -count=1` - all mocked-collaborator tests pass (including the new `sessionlifecycle` Manager tests); every repository-integration/concurrency test self-reports `SKIP` - this sandbox still has no reachable Postgres/Docker engine (`docker ps` confirms the daemon is unreachable), the same known limitation recorded by every prior pass. `docs/work/active/WORK-0001-session-lobby-foundation.md` transitioned READY -> IMPLEMENTING (Status/Last status change updated); its Implementation Status Note, Verification Performed So Far, and Completion Record sections were updated to reflect this pass. **WORK-0001 is not DONE**: independent review per `docs/ai/protocols/IMPLEMENTATION_REVIEW.md` has not been performed, and the real-Postgres integration/concurrency tests remain unexecuted in this sandbox - both remain open gates. Slice 2 was not started.
Last durable checkpoint (transaction/outcome/mechanism-refinement handoff, 2026-09-17): Documentation-only handoff, immediately following the design-revision implementation pass above; **no Go source, tests, or migrations were touched**. A second implementation review found the just-implemented 2026-09-16 design still carried transitional ceremony worth removing before closure: (1) the Manager's injected `transactor` dependency (ceremony around the already-generic `utils.RunInDBTransaction`); (2) the `bizErr`/outer-scoped-result-variable pattern used to let a business rejection escape the transaction callback separately from its own return; (3) `AlreadyJoined`/`LobbyFull`/`LobbyExpired`/Leave's-equivalent declines modeled as returned Go `error`s rather than result-value outcomes on an already-correctly-evaluated operation; (4) `internal/repo`'s `LockSessionByID`/`LockSessionByUUID`/`ClaimSessionRequest`/`CompleteSessionRequest` forwarding methods, which only called `sessionlock`/`idempotency` with no persistence responsibility of their own; (5) `idempotency.Claim`'s redundant `claimed bool` and its provoke-unique-violation-then-recover-in-the-same-transaction Postgres mechanic; (6) the shared workflow-level `idempotency.go` file centralizing every step's payload/interpretation instead of each step owning its own; (7) `Row`-suffixed workflow-facing repository types/variables (`SessionRow`/`ActorRow`/`ParticipantRow`/`RequestRow`) instead of entity-named ones; (8) `now` parameters on `CreateActor`/`CreateSessionWithHost`/`CreateJoinCode`/`SetSessionTerminal` used only to stamp `created_at`/`updated_at`. Persisted refined reusable engineering standards: `docs/engineering/standards/error-handling.md` (new "Expected Business Outcome vs. Error" section - the general outcome-vs-error classification), `docs/engineering/standards/idempotency.md` (Token Semantics/Completed-Outcomes wording updated to "outcome" rather than "business error"; new "Claim Mechanism Contract" section - simplified `(requestID, existingRequest, error)` shape, `Row`->`Request` naming, Postgres `ON CONFLICT ... DO NOTHING RETURNING id`; Organizational Guidance extended with step-local interpretation/direct-mechanism-usage guidance), `docs/engineering/standards/repositories.md` (Transaction Ownership refined - no injected `transactor` dependency, new "Callback Contract: Generic Result, No Outer-Variable Mutation" subsection, business declines committed as normal successful results; Sharing Rule extended against forwarding-only repository methods; new "Naming Exposed To Workflow Code: Entities, Not Rows" and "Timestamp Ownership" sections), `docs/engineering/standards/function-signatures.md` (new "Callback-Based Helpers Return Values, Not Mutate Closures" section), and `docs/engineering/standards/INDEX.md` (descriptor lines updated). Created `game/docs/decisions/GAME-ADR-0022-session-lobby-business-declines-as-workflow-outcomes.md` (ACCEPTED, refines - does not supersede - GAME-ADR-0021/GAME-ADR-0004: Join's/Create's/Leave's already-accepted deterministic business declines are represented as result-value outcomes rather than returned `error`s; token semantics and every other GAME-ADR-0021/GAME-ADR-0004 decision are unchanged) and updated `game/docs/decisions/INDEX.md` (next Game ADR is now `GAME-ADR-0023`) and `game/README.md`'s Join paragraph to describe `AlreadyJoined` (and the lobby's other declines) as workflow outcomes rather than as an application "business error." Revised `docs/work/active/WORK-0001-session-lobby-foundation.md` against these clarified rules (returned to DRAFT for the revision, then re-approved READY per the human's explicit approval of this handoff's decisions) - see its new "2026-09-17 Design Revision (Transaction/Outcome/Mechanism Refinement, HUMAN-APPROVED)" section for the full list of material changes and its correspondingly expanded Approved Design/Constraints/Acceptance Criteria. The existing Create/Join/Leave implementation was deliberately left untouched and now needs a further implementation pass to comply with this further-revised design before Slice 2 begins. `PLAN.md` was not touched (Slice 1 is still the active slice; this is a further refinement within it, not a slice change).
Last durable checkpoint (2026-09-17 Design Revision implementation pass, 2026-09-18): Implemented the 2026-09-17 Design Revision against the existing Create/Join/Leave code - see the full IMPLEMENTATION REPORT delivered at the end of this session for complete detail; summary here. `manager.go`: removed the `transactor` interface/`dbTransactor` implementation and the Manager's `tx` field; `Manager` now holds `db *gorm.DB` directly and implements `GetDB() *gorm.DB` itself (satisfying `utils.DBServicer`), so each step calls `utils.RunInDBTransaction(ctx, m, callback)` directly. Each step (`step_create.go`/`step_join.go`/`step_leave.go`) now calls `sessionlock.LockByID`/`LockByUUID` and `idempotency.Claim`/`Complete` directly instead of through removed `internal/repo` forwarding methods (`LockSessionByID`/`LockSessionByUUID`/`ClaimSessionRequest`/`CompleteSessionRequest`, all deleted, along with `internal/repo/request.go`). Each step's transaction callback now returns `(Result, error)` directly (no outer-scoped `result`/`bizErr` variables); the per-transaction business logic was split into an unexported `createSessionInTx`/`joinSessionInTx`/`leaveSessionInTx` method so mocked-collaborator unit tests can still exercise the pre-transaction logic without a real DB (the lock/claim mechanism calls further inside require a real Postgres connection and are proven by the existing `*_integration_test.go`/`manager_concurrency_test.go` files instead - see Local Implementation Decisions in the IMPLEMENTATION REPORT for why the previously-mocked in-transaction scenarios moved there). `types.go` gained `JoinOutcome`/`LeaveOutcome` value types and `Outcome` fields on `JoinResult`/`LeaveResult`; `JOINED`/`LOBBY_EXPIRED`/`LOBBY_FULL`/`ALREADY_JOINED` and Leave's `LEFT`/`NOT_IN_LOBBY`/`ACTOR_NOT_FOUND` are now returned as outcome values alongside a `nil` error, never as a Go `error` - the corresponding sentinel errors (`ErrLobbyExpired`/`ErrLobbyFull`/`ErrAlreadyJoined`/`ErrNotInLobbyPhase`/`ErrActorNotFound`) were removed from `game/session/session.go`. The shared `idempotency` package: `Row` renamed `Request`; `Claim` dropped `claimed bool` (mutually exclusive `requestID`/`existingRequest`); the Postgres mechanic changed from provoke-then-recover to a single `INSERT ... ON CONFLICT DO UPDATE ... RETURNING` upsert distinguishing a fresh claim from an existing one via the `xmax` system column (a local implementation decision instead of the WORK's illustrative `ON CONFLICT DO NOTHING RETURNING id` + fallback `SELECT`, made specifically because that literal illustrative approach can fail to observe a concurrently-committed row for the remainder of a REPEATABLE READ transaction - see Local Implementation Decisions in the IMPLEMENTATION REPORT for the full reasoning; this stays within the WORK's own "an equivalent PostgreSQL-safe mechanism providing the same guarantee is acceptable" allowance). The shared `sessionlock` package: `Row` renamed `Session`. Per-operation idempotency payload structs/interpretation functions moved next to their owning step (`createRequestPayload`/`interpretExistingCreateClaim` in `step_create.go`, etc.); only the mechanical `marshalPayload` helper remains shared (`payload.go`, replacing the deleted workflow-level `idempotency.go`). `internal/repo`: `ActorRow`/`ParticipantRow` renamed `Actor`/`Participant`; `CreateActor`/`CreateSessionWithHost`/`CreateJoinCode` dropped their audit-only `now` parameter (DB defaults now populate `created_at`/`updated_at`); `CreateParticipant`/`ActivateParticipant`/`DeactivateParticipant`/`RevokeActiveJoinCode` renamed their `now` parameter to the semantic value it actually sets (`joinedAt`/`leftAt`/`revokedAt`). **Local implementation decision (bug fix, not a design change)**: `CreateJoinCode`'s collision-retry loop had the same provoke-then-recover defect the WORK explicitly identified for the idempotency claim (a genuine Postgres unique-violation error aborts the whole Create transaction, breaking the retry loop) - fixed the same way, via a non-error `ON CONFLICT (code) WHERE revoked_at IS NULL DO NOTHING RETURNING id` against the existing partial unique index. Updated/added tests throughout (see the IMPLEMENTATION REPORT); regenerated `mocks_test.go` via `mockgen`. Updated `game/docs/FLOWS.md`'s Create/Join/Leave sequence diagram and prose, and `game/CURRENT_STATE.md`'s one stale `idempotency.go` file reference, to describe the new structure - no schema/`DATA_MODEL.md` change was needed (schema unchanged). Verification: `go build ./...`, `go vet ./...`, `gofmt -l` (changed files only) all clean repository-wide; `go test ./... -count=1` - all mocked-collaborator/compile-checked tests pass, every real-Postgres repository-integration/concurrency test (including two new ones added directly against the shared `sessionlock`/`idempotency` packages) self-reports `SKIP` - this sandbox still has no reachable Docker/Postgres engine (`docker ps` confirms), the same known limitation recorded by every prior pass. `docs/work/active/WORK-0001-session-lobby-foundation.md` `Status:` was left at `IMPLEMENTING` (not advanced to DONE) - independent review per `docs/ai/protocols/IMPLEMENTATION_REVIEW.md` has not been performed, and the real-Postgres tests remain unexecuted; both remain open gates. Slice 2 was not started. **Same-session readability touch-up (still 2026-09-18, before independent review)**: per direct human feedback, renamed `createInTx`/`joinInTx`/`leaveInTx` to `createSessionInTx`/`joinSessionInTx`/`leaveSessionInTx`; each now receives only the payload struct (marshals to bytes internally) instead of both the struct and its pre-marshaled bytes; each `interpretExisting*Claim` helper was moved below the method that calls it in its file (was previously the first thing defined in each `step_*.go` file); and the Manager's `now func() time.Time` field/callback was removed entirely in favor of calling `time.Now().UTC()` directly where needed. Pure naming/structure/readability changes, no behavior change - re-verified clean (`go build`, `go vet`, `go test ./... -count=1`) after applying them.
Last durable checkpoint (real-Postgres verification and closure pass, 2026-09-18): Independent implementation review per `docs/ai/protocols/IMPLEMENTATION_REVIEW.md` had already completed with verdict **APPROVED** before this session began. This session executed the one remaining closure gate: real-Postgres repository-integration/concurrency test verification, unexecuted in every prior sandbox used across this WORK's entire history because none had a reachable Docker/Postgres engine. This sandbox does: `playhoot-postgres-1` (`postgres:17-alpine`) was already running via the repository's own `docker-compose.yaml`; its actual credentials were read from the running container (`docker inspect`) and exported as `TEST_DATABASE_HOST=localhost`/`TEST_DATABASE_PORT=5432`/`TEST_DATABASE_USERNAME=playhoot`/`TEST_DATABASE_PASSWORD=DBPLAYHOOT`/`TEST_DATABASE_NAME=playhoot`/`TEST_DATABASE_SSL_MODE=disable`. `go test ./game/session/... -count=1 -v` then ran **every** repository-integration and concurrency test end-to-end against real Postgres for the first time - all pass, none skipped, including the concurrent-Create idempotency-claim test, the concurrent-Join-for-final-slot/Join-racing-Leave/operation-racing-lobby-expiration tests, and the shared `sessionlock`/`idempotency` package's own concurrency tests. `go build ./...` and `go vet ./...` remain clean. Running the broader `go test ./... -count=1` (wider than this WORK's own required `./game/session/...` scope) surfaced two failures never previously observable in any sandbox, both caused by the identical defect: `game_definitions.script`/`games`-adjacent tests assert byte-exact string equality against a `JSONB` column's value, but PostgreSQL's `jsonb` type re-serializes stored JSON into its own canonical text form (a space after `:`/`,`), so a semantically-identical round-trip fails a literal string comparison. One of the two (`game/game/usecases/getgamedefinition`'s `TestRepoGetGameDefinitionByUUID`) is the new Game Management read capability this WORK itself added, so it was fixed in-scope: `require.Equal(t, *tc.expectedDefinition, *got)` was replaced with `require.Equal(t, tc.expectedDefinition.UUID, got.UUID)` + `require.JSONEq(t, tc.expectedDefinition.Script, got.Script)` (semantic JSON comparison) - a test-assertion-only change, no production code/migration/behavior changed; re-run passes. The other (`game/game/usecases/getgame`'s `TestRepoGetGameCurrentVersion`) is pre-existing Game Management code this WORK never touched and is explicitly outside WORK-0001's authorized scope ("Any Game Management change beyond the one minimum read capability... is out of scope") - it was left unfixed and is recorded as a discovered, non-blocking, pre-existing test-quality defect for a separate future follow-up, not silently ignored and not fixed here. No implementation defect was found anywhere in Session Runtime code itself; this closes the real-Postgres verification gate purely by running already-correct tests against a now-reachable database, plus one one-line test-assertion fix in code this WORK itself introduced. `docs/work/active/WORK-0001-session-lobby-foundation.md` was updated (new "Verification Performed So Far (2026-09-18 real-Postgres verification pass, post-independent-review)" subsection, Implementation Status Note's gate-tracking sentence, and a filled Completion Record), its `Status:` set to `DONE`, and the file moved (`git mv`) to `docs/work/completed/WORK-0001-session-lobby-foundation.md`. `PLAN.md` was updated: Slice 1 marked DONE, Slice 2 marked unblocked-but-not-started, and the WORK References/Recommended First Slice sections synchronized to point at the completed WORK. This file's resume header (above) was updated to reflect Slice 1's closure and that this workspace is now between slices, with Slice 2 the next just-in-time candidate - **not started, not materialized into a WORK, per this session's explicit scope.** No Session Runtime production Go code was changed by this session - the only code change was the one-line `getgamedefinition` test-assertion fix described above.

Last durable checkpoint (isolated `game/game` -> `game/management` rename, 2026-09-18): Out-of-band, human-directed, immediately after WORK-0001's closure and explicitly before Slice 2 - `WORK-0002-rename-game-management-package.md` (DONE same day). Pure structural rename with independent review APPROVED after one fixed REQUIRED_FIX (gofmt import ordering); no product/architecture/domain change; no change to this workspace's own design. Recorded here only because it changes the physical path Slice 2 code must reference (`game/management/...`, not `game/game/...`).

Last durable checkpoint (Slice 2 reconciliation and WORK-0003 draft, 2026-09-19): Reconciliation: verified Slice 1 tracking is cleanly closed (`docs/work/completed/WORK-0001-session-lobby-foundation.md` present and DONE; `docs/work/active/` contains no stale non-terminal Slice 1 remnant) and that the `game/game` -> `game/management` rename (WORK-0002, DONE) left no stale current-state/live-standard/Go-source reference - a repository-wide `grep -rn "game/game"` found exactly one remaining hit outside WORK-0002's own file and the already-exempt historical set (ADR-0001, GAME-ADR-0001, WORK-0001): `PLAN.md`'s own "Actual Implementation Drift Found" section, dated/labeled HISTORICAL from the 2026-09-08 pre-Slice-1 inspection - correctly left unchanged, since it is a historical record of what was true before Slice 1/the rename, not a current-state claim (`docs/decisions/README.md`'s Historical Immutability principle applied the same way here as to a decision record). No tracking updates were needed for the rename beyond what WORK-0002 itself already made. Design review: read the full accepted architecture relevant to Start (`game/README.md`'s Lobby Lifecycle Contract/RUNNING Mutation Serialization/Turn And Persistence Model/Failure Classification/Terminal Cleanup sections, `SESSION_RUNTIME_PERSISTENCE_MODEL.md`, GAME-ADR-0004/0005/0006/0007/0017/0018/0019/0021/0022), the actual Slice 1 implementation (`sessionlifecycle.Manager`/`step_create.go`/`step_join.go`/`internal/repo`/`sessionlock`/`idempotency`, all unchanged in shape and directly reusable), and the Game Language engine's actual public API (`engineservice.Compile`/`NewSnapshot`/`Step`/`EncodeSnapshot`, `engine.Limits`/`InitializationInput`/`UserValue`/`UserID`) to ground Slice 2's design in what the engine and Slice 1 code actually expose today, not merely what the accepted documents describe abstractly. Confirmed no material product/architecture/domain question remains unresolved that would block drafting a WORK - every open item found is an implementation-planning-level gap within already-ACCEPTED architecture (matching this workspace's own "Deferred Design Topics" list, which already classifies exactly this kind of item as non-blocking). Materialized `docs/work/active/WORK-0003-session-start-first-runtimeturn.md` (Status DRAFT) with a concrete Approved Design: `Start` as a new step on the existing `sessionlifecycle.Manager` (`step_start.go`), reusing the existing transaction/lock/idempotency pattern unchanged; a new shared internal mechanism package `game/session/internal/runtimeturn` (justified under `repositories.md -> Sharing Rule`, parallel to `sessionlock`/`idempotency`) as the common RuntimeTurn executor - Step-draining, the `MAX_STEPS_PER_RUNTIME_TURN = 20` bound, and Turn/Step/State persistence - that Slices 3/5/6 are expected to reuse unchanged; full accepted-shape migrations for `session_runtime_state`/`session_runtime_turns`/`session_runtime_steps` created once (unused `source_interaction_id`/`source_timer_obligation_id` columns included, left NULL until Slices 3/5); the pre-first-Turn fatal path reusing Slice 1's existing `SetSessionTerminal` (no `session_runtime_failures` table - correctly deferred to Slice 6); and Start's own outcome/idempotency shape extending GAME-ADR-0022's already-accepted pattern. Four items were deliberately left as explicit WORK-0003 Blockers rather than silently decided, since each shapes a pattern later slices inherit or fills a genuine gap neither GAME-ADR-0017 nor GAME-ADR-0019 spells out verbatim: (1) introducing the shared `runtimeturn` package now vs. later; (2) the `RUNTIME_STATE_INVALID` (pinned-Definition-recompile-failure) vs. `RUNTIME_EXECUTION_FAILED` (everything else, including an outright rejection of Start's own initial signal) classification split; (3) treating a fatal `StartOutcomeRuntimeInitFailed` as a normal replayable idempotency outcome; (4) defaulting `players` roster ordering to join order with no existing product decision on record. `PLAN.md` was updated (Slice 2 status, WORK References, WORK-0002 added) and this file's resume header was updated accordingly. `docs/ai/workspaces/active/session-runtime-v1/HUMAN_REVIEW.md` was rewritten with this checkpoint's proposal and the four Blockers, superseding its prior (Slice-1-era) READY-approval record. **No production Go code, tests, or migrations were written or changed by this session** - this was reconciliation plus WORK drafting only, exactly as scoped. WORK-0003 was deliberately left DRAFT, not READY - only the human may authorize that transition. Slice 2 implementation was NOT started.

Last updated: 2026-09-19

# Resume Context

This workspace preserves the active initiative `session-runtime-v1`.

Goal: design and then incrementally implement the complete Session Runtime lifecycle. This is prioritized because playable multiplayer sessions are a core initial product capability.

HISTORICAL NOTE (superseded by the current resume header above): the sentence that used to stand here said the initiative was still in Architecture Discussion with no Feature Development/WORK opened. That was true only for the earliest checkpoints of this workspace. Architecture Discussion is now CLOSED (human-approved), the initiative is in Feature Development for Slice 1 (see the resume header), and `WORK-0001-session-lobby-foundation.md` exists as DRAFT. Do not treat this paragraph as current state - it is preserved only as chronology.

This workspace records process continuity only. Accepted architecture/domain facts were promoted to canonical owners and ADRs where required. This file is not canonical implementation authority.

## Accepted Foundation Decisions

Status: HUMAN-APPROVED and canonically promoted.

Session Runtime owns all durable authoritative state that determines the meaning of a session and must be reconstructible after loss or restart of the process. `Stateless` means process-stateless/reconstructible, not that the domain itself has no state.

`Live Session Coordinator` is the conceptual responsibility boundary outside Session Runtime. The Coordinator owns ephemeral/runtime mechanisms such as live connection bindings, delivery/fan-out to connected clients, detection of physical disconnects, and physical timer/scheduling mechanisms. It does not own authoritative session/game truth or business consequences. This is a responsibility boundary, not a requirement to create another deployed service. V1 may keep it in the same Go process.

V1 does not introduce sticky-session correctness requirements, Redis, distributed session routing, distributed locks, or other multi-instance mechanisms. The initial single-process modular-monolith deployment may use local maps, channels, and Go timers for ephemeral mechanisms. Correctness of a session must not depend exclusively on those ephemeral objects.

Timer obligations that can affect game semantics are durable Session Runtime state. Coordinator owns the physical timer and knowledge that wall-clock time elapsed; Session Runtime owns the durable timer obligation and decides the semantic consequence. When a timer expires, Coordinator calls Session Runtime with the corresponding expiration signal/event. Pending or overdue obligations must be reconstructible/recoverable from durable state - this means the obligation and its configured delay survive, not that the exact original overdue/elapsed wall-clock position is reconstructed. GAME-ADR-0008 later accepted the concrete V1 tradeoff: no durable absolute deadline, full-configured-delay rescheduling on recovery.

Identity owns stable `User` identity and public `UserUUID`. A guest is already a `User`; normal guest-to-registered conversion preserves the same `UserUUID`. Session Runtime owns local `SessionActorID`. A SessionActor persistently correlates to `Identity.User` through `user_uuid`.

Public/app Session Runtime boundaries use an authenticated `UserUUID`; clients are not trusted sources of `UserUUID`. A trusted auth/application layer resolves credentials to `UserUUID`. Identity/Auth proves who the caller is; Session Runtime decides what the caller may do inside a Session.

`SessionActorID` is internal-only and is not a public/app contract. Coordinator, Orchestrator, adapters, and clients do not own the mapping. Host and Participant are independent relationships to Session-owned actor identity.

Canonical references: GAME-ADR-0002, GAME-ADR-0003, ADR-0005, IDENTITY-ADR-0001, GAME-ADR-0005, `ARCHITECTURE.md`, `identity/README.md`, `game/README.md`, and `docs/engineering/standards/cross-domain-reference-naming.md`.

## Accepted Lobby Lifecycle Contract

Status: HUMAN-APPROVED and canonically promoted.

Session-scoped lobby operations that materialize Session meaning serialize per Session. Join, Leave, Start, and expiration materialization are mutually serialized while the Session is in LOBBY. A database row lock is the natural V1 mechanism but not the canonical requirement. HISTORICAL NOTE: this paragraph originally scoped the serialization guarantee to LOBBY only; that scoping was later extended - the same per-Session DB-locking guarantee now also covers RUNNING (see Accepted Session RUNNING Mutation Serialization below, GAME-ADR-0018). Do not read "LOBBY only" as current truth.

Create Session/Room takes Game public UUID, authenticated host UserUUID, and opaque idempotency key. The caller does not provide an external `engine.Program`. Session Runtime resolves the current playable immutable Game definition through the narrow Game Management read capability, pins it, and may compile/validate outside the Session transaction. In the Session transaction it creates the LOBBY Session, host SessionActor correlated to `user_uuid`, host relationship, active JoinCode, and `lobby_expires_at`. The host is not automatically a Participant. QR codes and URLs are projections over JoinCode state.

JoinCode is authoritative Session Runtime state, not cache state. One active JoinCode exists per join-admissible lobby. It is revoked when the Session leaves LOBBY. Historical codes may be retained and textual code values are not forever reserved.

Join takes JoinCode, authenticated UserUUID, display-name snapshot from a trusted app/integration boundary, and opaque idempotency key. It resolves the JoinCode, obtains the pinned Game definition for constraints, enters serialized mutation, revalidates LOBBY and expiration, finds or creates the SessionActor for `(Session, UserUUID)`, and creates or reactivates Participant state. Repeated active Join has the same admission effect. Active Participants must not exceed `players.max` from the pinned Game definition.

Leave is a LOBBY participation operation. The public caller is `UserUUID`, resolved to SessionActor. It deactivates Participant and releases the slot, but does not delete SessionActor, remove host, or cancel the Session. A participating host may leave gameplay participation but remains host. Rejoin to an admissible lobby reuses the same SessionActor.

`lobby_expires_at` is authoritative. Join and Start correctness do not depend on a sweeper. If phase is LOBBY and now is at or after the deadline, an operation may atomically materialize TERMINAL with a lobby-expiration terminal reason, terminal timestamp equal to the deadline, and JoinCode revocation.

Start takes Session UUID, authenticated UserUUID, and opaque idempotency key. It resolves `(SessionUUID, UserUUID) -> SessionActorID`, verifies host authority, uses the pinned immutable definition/version, validates LOBBY, expiration, host, active Participants, `players.min`, and defensively `players.max`, initializes engine state, processes the engine-required initial signal/first Commit per engine contract, persists authoritative runtime state and durable consequences, atomically transitions to RUNNING, sets `started_at`, revokes JoinCode, and commits before external outputs. Failure must not produce partial RUNNING. Coordinator/client outputs occur after durable commit.

External mutating Session commands Create, Join, Leave, and Start accept an opaque idempotency key. Retries of the same logical command must not repeat effects. The final idempotency schema/algorithm remains undesigned.

Canonical references: GAME-ADR-0004, GAME-ADR-0005, and `game/README.md`.

## Accepted Game Language Root Roster Contract

Status: HUMAN-APPROVED and canonically promoted; not yet implemented as a complete validated contract.

Session Runtime supplies this standardized root input at Start:

```text
players: list<user>
```

Session Runtime builds `players` from active Participants at Start. Each `user` corresponds to Session-local runtime identity derived from SessionActorID. Identity.UserUUID is never exposed to authored Game Language or engine semantics.

Authored games should use the standard `players` root parameter instead of inventing custom root parameter names for the participant roster.

For V1, arbitrary external game-specific root parameters are deferred until Session Configuration is designed.

Canonical references: GAME-ADR-0006, `game/language/v1/program/README.md`, `game/language/v1/engine/README.md`, and `game/language/v1/engine/LOGICAL_CONTRACT.md`.

## Accepted Runtime Turn, Persistence Model, Timer Recovery, And Archival Direction

Status: HUMAN-APPROVED and canonically promoted.

`RuntimeTurn` is the historical/transactional unit for RUNNING-phase execution: one external/runtime cause, 1..N internal `engine.Step` calls in one transaction, one resulting Session-state `sequence`, and one authoritative final Snapshot. `RuntimeStep` is technical-only execution history beneath a Turn and does not own a Session-state sequence.

The accepted persistence model adds `session_requests` (idempotency, replacing the earlier proposed `session_command_receipts` name, storing `request_payload` rather than a `request_hash`), `session_runtime_turns`, `session_runtime_steps`, `session_runtime_state` (a thin pointer to `current_turn_id`, no duplicated Snapshot), `session_interactions`, and `session_timer_obligations`. Interaction and Timer durability/recovery is modeled through Turn-level references (`opened_by_turn_id`/`created_by_turn_id`, `closed_by_turn_id`, `source_interaction_id`/`source_timer_obligation_id`), not Step-level or sequence-range bookkeeping.

V1 does not persist an absolute due-at/deadline for Game Language timers and does not include a durable Coordinator-owned `live_timer_schedules` table. Session Runtime persists only the timer obligation and its configured delay; on recovery, active obligations may be rescheduled using their full configured delay from the new scheduling moment, and preserving elapsed wall-clock time across a full process restart is not required for V1. `lobby_expires_at` is unaffected and remains a separate, already-accepted Session lifecycle deadline.

Long-term runtime-history archival direction is accepted: PostgreSQL remains the hot/runtime store; a versioned JSON artifact may eventually be written to long-term object storage (for example GCS); `session_history_archives` tracks this per Session with a provider/key identifier (not an expiring URL). Heavy runtime/history tables (`session_runtime_state`, `session_runtime_turns`, `session_runtime_steps`, `session_interactions`, `session_timer_obligations`) may become an explicit hard-delete exception, but only after the archive is successfully written and verified (`READY` status, checksum). `session_actors` and `session_participants` are excluded from this deletion policy and remain relationally stored indefinitely for product queries. The concrete JSON archive schema and GCS implementation remain DEFERRED.

Canonical references: GAME-ADR-0007, GAME-ADR-0008, GAME-ADR-0009, `game/README.md`, and `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (full accepted table/column/relationship diagram).

## Accepted Disconnect, Reconnect, And Resynchronization Boundary

Status: HUMAN-APPROVED and canonically promoted.

Three responsibilities stay distinct and must not collapse into one owner: the transport fact that a physical connection disappeared/returned (Coordinator); the platform/runtime semantic event that a Session User is now considered disconnected/reconnected (Session Runtime boundary); and the gameplay consequence of that event (Session Runtime + Game Language).

A physical disconnect does not deactivate `session_participants`, remove a Participant, free a slot, delete a SessionActor, or terminate the Session; Session Runtime does not add authoritative connection-state fields (`is_connected`, `connection_id`, `websocket_id`). The Coordinator may apply a short, in-memory transport grace/debounce period (duration not decided here, implementation-level/configurable) to absorb transient failures; a reconnect within grace rebinds the connection and cancels escalation with no semantic disconnect ever observed and no RuntimeTurn created. If grace expires without reconnection, the Coordinator notifies Session Runtime of a semantic disconnect; the Coordinator never decides gameplay consequences (remove/forfeit/pause/terminate/continue) - those belong to Session Runtime/Game Language. The exact Game Language disconnect contract/name is not approved; no name (for example `PlayerDisconnected`) is frozen as syntax/API.

Reconnect is not Join: Session Runtime resolves `(SessionUUID, UserUUID) -> existing SessionActor`, reusing existing SessionActor/Participant identity without creating a new slot, requiring a JoinCode, or rerunning lobby admission. Transport reconnect does not imply gameplay reinstatement - if the game already reacted to the semantic disconnect (removed/forfeited/disabled), a transport-level reconnect does not itself undo that; a corresponding reconnect semantic event may need to reach Session Runtime/Game Language once that contract exists.

No new durable connection-state/grace table is introduced for V1; live connection bindings and transport-grace state remain ephemeral Coordinator state, and existing durable state (SessionActor, Participant, current RuntimeTurn/Snapshot, active Interactions/TimerObligations) remains sufficient.

Session Runtime exposes a resync read capability keyed by `(SessionUUID, UserUUID)` (exact transport/DTO not frozen) that returns a player-facing Session projection - never the raw `engine.Snapshot`, and never leaking SessionActorIDs/engine paths/slots - carrying the current RuntimeTurn `sequence` as its monotonic version (no second version counter). Reconnect during transport grace produces no RuntimeTurn and is a pure resync read; reconnect after an already-escalated semantic disconnect processes gameplay/runtime semantics first (through the future Game Language contract, which may produce a RuntimeTurn) and only then builds resync from the resulting current state.

Game-defined disconnect/reconnect policy (continue/wait/timer/forfeit/remove/pause/end - examples only) remains unresolved and is the next design topic; no signal name, syntax, required handler, default policy, timeout, or automatic removal semantics is invented by this checkpoint.

Canonical references: GAME-ADR-0010, `game/README.md` (Session Runtime Disconnect, Reconnect, and Resynchronization Boundary section).

## Accepted Game Language Disconnect/Reconnect Authored Semantics

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

`UserDisconnected` and `UserReconnected` are standard `NamedSignalSource` platform/lifecycle signals (not new `SignalKind` variants), each exposing exactly one authored field `user: user` - Session-local runtime identity derived from `SessionActorID`, never `Identity.UserUUID`, connection/socket IDs, IP, timestamps, transport-grace information, or Coordinator internals. The pre-existing empty-schema `UserDisconnected` placeholder in `program/signal.go`/`compile_signals.go` is compatible scaffolding whose intended schema is now `user: user`; `UserReconnected` is newly accepted conceptually and does not exist in current code.

Session Runtime delivers both signals to the root workflow only - no implicit broadcast to nested child/task-group/ask-group instances, since the engine's `Step` contract addresses one instance path per call with no hidden multi-instance fan-out; nested workflows may later be coordinated explicitly by authored logic or a future language capability.

Handling either signal is optional. An unhandled signal is an ordinary rejected/unmatched signal (`ErrSignalRejected`) with no automatic gameplay consequence - Session Runtime must not infer remove/forfeit/pause/skip/end - and does not create a RuntimeTurn solely to record a no-op. "The game ignores disconnect" means only that disconnect itself does not mutate gameplay; it does not guarantee continued gameplay progress.

An interaction targeted at a SessionActor with no live transport connection is still created as a normal durable SessionInteraction and remains logically ACTIVE; Session Runtime must never suppress, auto-answer, skip, or redirect it, or deactivate the Participant, merely because the actor is offline (`logical interaction existence != successful live delivery`). Progress while an actor is offline is ordinary authored Game Language policy (e.g., an authored timer/timeout), not a Session Runtime inference; if no timeout applies and the player never reconnects, execution may legitimately remain waiting, and Session Runtime must not "intelligently" invent progress. HISTORICAL NOTE: this paragraph originally left "a future max-runtime/inactivity/runaway protection" as deferred; that protection is no longer deferred - durable inactivity expiration (GAME-ADR-0014) and the RuntimeTurn Step-chain execution bound (GAME-ADR-0019) now accept exactly this kind of protection, both still honoring the invariant stated here that it must never fabricate gameplay responses or silently perform authored actions.

Canonical references: GAME-ADR-0011, `game/README.md` (Authored Game Language Disconnect/Reconnect Contract subsection), `game/language/v1/program/README.md`, `game/language/v1/engine/README.md`, `game/language/v1/engine/LOGICAL_CONTRACT.md`.

## Accepted Game Language Keyed Timer Slot Capability

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

The existing ordinary `TimerSlotDeclaration` holds at most one pending timer per statically named slot per workflow instance, which was found insufficient for a root-level policy needing multiple simultaneous independent timers (for example, a per-player disconnect timeout for P1 and P2 at once). This is not solved as a Session Runtime special case; Game Language instead gains a general `KeyedTimerSlot<Key>` capability - independently addressable pending timers identified by `(workflow instance/path, slot, key)`, at most one timer per exact tuple, with different keys fully independent. Scheduling into an already-occupied `(slot, key)` is an execution error with no implicit reset/replace/coalesce, mirroring the existing `TimerSlot` occupied-slot rule; cancellation affects only the selected key and is idempotent when none is pending; expiration exposes the authored key (conceptually `KeyedTimerExpired(slot)` carrying `key: KeyType`) without ever exposing internal timer-obligation UUIDs, wall-clock scheduling data, or database identifiers. This is a general primitive - also usable for cooldowns, team timers, per-object timers, keyed negotiations - explicitly not a `DisconnectTimer` special case; disconnect motivated but does not own the capability. Naming/API/Go type names are not frozen.

The accepted Session Runtime persistence model gains a nullable `session_timer_obligations.engine_key` discriminator (null for ordinary timers, populated for keyed timers) so a keyed timer's expiration can be reconstructed with the correct authored key on recovery; this is internal Session/engine routing metadata, never exposed to Coordinator/frontend merely because it is persisted. The long-term archive must eventually preserve whatever key metadata is necessary to replay archived keyed-timer history; the concrete archive JSON format remains deferred.

Canonical references: GAME-ADR-0012, `game/README.md` (Keyed Timer Slots subsection, and the Session Runtime Turn And Persistence Model section), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (Keyed Timer Discriminator section), `game/language/v1/program/README.md`, `game/language/v1/engine/README.md`, `game/language/v1/engine/LOGICAL_CONTRACT.md`.

## Accepted Process-Agnostic Recovery And Durable Inactivity Expiration

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

Session Runtime does not model durable ownership of a Session by a particular process/pod/runtime instance: no `owner_process_id`, no runtime instance ownership, no process heartbeat ownership, no fencing/takeover generation introduced solely for process ownership, no sticky Session ownership, no durable `RECOVERING` phase. A process crash by itself is not a Session-domain event and does not change `Session.phase`. The accepted RuntimeTurn transactional model (GAME-ADR-0007) is preserved for crash recovery: an uncommitted Turn rolls back entirely via ordinary database transaction atomicity (no durable `RuntimeTurn = PROCESSING`, no resuming "Step N"); a committed Turn remains fully authoritative regardless of which process executed it or what happened immediately after commit. A process resuming a `RUNNING` Session reconstructs current state from `sessions`, `session_runtime_state.current_turn_id`, the final Snapshot on that Turn, active `session_interactions`, and active `session_timer_obligations` - never by replaying RuntimeTurn `1..N` history, which remains useful only for history/debugging/archive/audit-replay tooling. Game Language timer recovery continues to use the GAME-ADR-0008 full-configured-delay tradeoff unchanged.

Every `RUNNING` Session gets a durable inactivity deadline, `sessions.activity_expires_at`, distinct from `lobby_expires_at` and not a process ownership lease. It is renewed (`= now + inactivity_ttl`, TTL configurable operational/product policy, V1 illustrative value ~10 minutes, never hard-coded into Game Language) only by operations that constitute meaningful evidence of continued active use (RuntimeTurn-producing gameplay, accepted interaction processing, timer expiration processing, meaningful lifecycle/runtime events, justified reconnect/resume activity - illustrative, not exhaustive); passive reads/polling must not renew it. `activity_expires_at` itself is the authoritative source of truth for expiration, not Reaper discovery, mirroring the already-accepted `lobby_expires_at` pattern (GAME-ADR-0004). Every active-dependent operation validates it under the normal per-Session serialization mechanism before processing and potentially renewing; an operation arriving after the deadline must not revive the Session merely because persisted `phase` still reads `RUNNING`, and may instead atomically materialize, in the same transaction, `phase = TERMINAL`, `terminal_reason = RUNTIME_INACTIVITY_EXPIRED`, `terminal_at = activity_expires_at`, then reject the attempted action - never renewing, reopening, fabricating gameplay, or creating a RuntimeTurn merely to represent expiration. A background Session Reaper proactively finds already-expired-but-unmaterialized Sessions and materializes them under the same serialization/revalidation rules, but does not itself decide expiration; if a legitimate operation already won the race and renewed the deadline, the Reaper rechecks and does nothing - the deadline, not Reaper scheduling latency, decides the outcome. `terminal_at` always equals `activity_expires_at`, never the Reaper's or a lazy operation's current wall-clock time (a multi-day platform outage does not move the semantic terminal instant). `terminal_reason = RUNTIME_INACTIVITY_EXPIRED` asserts only that the allowed inactivity period was exceeded - not a process crash, pod kill, host disconnect, or that every participant left - and is deliberately not named `PROCESS_CRASH`/`RUNTIME_ORPHANED`. The Archive Worker (GAME-ADR-0009) remains completely separate: it consumes only already-materialized `TERMINAL` Sessions per retention policy and must never inspect process ownership, detect crashes, determine RUNNING inactivity, or interpret `activity_expires_at`. Once `TERMINAL`, future gameplay timer/interactions are no longer executable as active gameplay; exact closure semantics for still-open Interactions/TimerObligations remain deferred, and materializing expiration must never fabricate engine responses or RuntimeTurns.

Canonical references: GAME-ADR-0013, GAME-ADR-0014, `game/README.md` (Session Runtime Process-Agnostic Recovery and Session Runtime Durable Inactivity Expiration sections), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (`sessions.activity_expires_at`, Process-Agnostic Recovery section, RUNNING Inactivity Deadline section).

## Accepted Session Actor Semantic Presence And Phase-Dependent Lobby-To-Runtime Membership

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

`session_actors` gains a durable `semantic_presence` field (`CONNECTED | DISCONNECTED`), conceptually owned by `session_actors` rather than `session_participants`, answering only "has this SessionActor crossed the Coordinator -> Session semantic connected/disconnected boundary?" This is distinct from `session_participants.active`, which answers "does this actor currently occupy/admit a participant position?" - the two may legitimately diverge (a reconnecting actor that is semantically `CONNECTED` while locked out of a full lobby). Physical connection state (socket IDs, connection IDs, live WebSocket presence, Coordinator bindings, grace timers, process IDs) remains entirely ephemeral Coordinator state; none of it is persisted by this decision, and GAME-ADR-0010's rejection of `is_connected`/`connection_id`/`websocket_id` persistence is unchanged.

After Coordinator grace expires, Coordinator reports the semantic disconnect using trusted Session identity; Session Runtime serializes the SessionActor `CONNECTED -> DISCONNECTED` transition per Session, and repeated disconnect reports while already `DISCONNECTED` are idempotent with respect to semantic state and must not duplicate Game Language disconnect effects. The consequence is phase-dependent:

In `LOBBY`, semantic disconnect deactivates the active Participant and releases its slot - the disconnected user no longer counts toward current lobby capacity, and no `UserDisconnected` exists yet because no game runtime/root workflow has started. This is intentionally stronger than RUNNING: a player still inside Coordinator transport grace is still semantically `CONNECTED` and keeps their active lobby slot. Reconnect reuses the same SessionActor/existing Participant record (not a new Join identity) and transitions `DISCONNECTED -> CONNECTED`; if the prior disconnect deactivated the Participant, re-admission is revalidated against current lobby constraints (`LOBBY` phase, not expired, current participant capacity against the pinned Game Definition `players.max`, other applicable accepted constraints) exactly as Join would validate. If admission succeeds, `Participant.active = true`; if it fails, the SessionActor may remain `CONNECTED` while the Participant stays inactive with no slot reserved - reconnect does not guarantee recovery of a previously released slot (for example: P1 disconnects and releases the last slot, P2 joins and occupies it, P1 reconnects but the lobby is full).

Start (already serialized per Session) builds the Game Language root `players: list<user>` strictly from Participants active at that serialized moment; a `DISCONNECTED` actor, or a `CONNECTED` actor not currently re-admitted as an active Participant, is excluded. The Coordinator-grace-vs-Start race is resolved entirely by the existing per-Session serialization (GAME-ADR-0004) with no special-casing outside Session: if grace has not yet expired when Start wins serialization, the Participant is still active and included; if the semantic disconnect was already processed first, it is excluded. An actor excluded from the Start-time roster does not join the running game merely by reconnecting afterward - Session Runtime must not deliver `UserReconnected` toward the Game Language runtime, or dynamically add, a SessionActor absent from that execution's `players` roster; post-Start late admission remains a distinct, separately deferred capability, not designed here.

In `RUNNING`, semantic disconnect does not deactivate/remove the Participant from the runtime roster: `semantic_presence` becomes `DISCONNECTED`, the SessionParticipant remains logically part of the running Session, Session Runtime may emit the already-accepted root `UserDisconnected(user)` (GAME-ADR-0011), and authored Game Language - not Session Runtime - determines gameplay consequences; Session Runtime must not free a runtime slot, silently remove the player, auto-forfeit, or auto-skip. Reconnect of such a runtime member may produce the already-accepted `UserReconnected(user)` transition before resync.

These three concepts must not collapse: `SessionActor.semantic_presence` (semantic connectivity, durable, phase-independent); `SessionParticipant.active` during `LOBBY` (currently admitted/Start-eligible); and the Game runtime roster after Start (immutable initial set from active Participants at Start, absent a future separately-approved dynamic-membership feature).

Canonical references: GAME-ADR-0015, `game/README.md` (SessionActor lifecycle paragraph, Session Runtime Lobby Lifecycle Contract section, Session Runtime Disconnect/Reconnect/Resynchronization Boundary section), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (`session_actors.semantic_presence` in both ER diagrams, "SessionActor Semantic Presence vs Participant Admission" section).

## Accepted Session Semantic Presence Recovery After Total Coordinator State Loss

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

Total loss of ephemeral Coordinator/process state does not itself mutate `session_actors.semantic_presence`: durable `CONNECTED`/`DISCONNECTED` values before the loss remain unchanged merely because a process crashed, sockets disappeared, or Coordinator maps/timers/graces were lost. Session Runtime remains process-agnostic (GAME-ADR-0013); no process ownership, process IDs, process heartbeat state, `RECOVERING`/`UNKNOWN` presence value, or durable socket state is introduced. `semantic_presence = CONNECTED` means only "Session Runtime has not yet processed a semantic `CONNECTED -> DISCONNECTED` edge for this actor," not "a live socket exists right now" - physical connectivity remains entirely Coordinator-owned ephemeral state (GAME-ADR-0010).

When Coordinator reconstructs/reconciles a still-semantically-active Session and finds a SessionActor with `semantic_presence = CONNECTED` but no current physical binding, it starts a fresh transport/recovery grace for that actor - the same transport-grace mechanism already accepted for ordinary transient disconnects, not a distinct process-crash-disconnect event or API. If the actor establishes a new authenticated connection before that fresh grace expires, Coordinator binds it and cancels the grace; the SessionActor remains `CONNECTED`; no `UserDisconnected`/`UserReconnected` is emitted; Coordinator resends normal resync state - preventing ordinary process restarts/deployments from producing artificial disconnect/reconnect pairs for every still-connected player. If the fresh grace expires without a binding, Coordinator invokes the normal semantic-disconnect path exactly as for an ordinary transport-grace expiry (no special process-loss API), and Session Runtime applies the already-accepted phase-specific rules (GAME-ADR-0015): LOBBY deactivates the Participant and releases its slot with no `UserDisconnected` yet; RUNNING preserves runtime membership and processes root `UserDisconnected(user)`.

A durably `DISCONNECTED` actor receives no recovery grace, since there is no semantic uncertainty to absorb; a later connection uses the already-accepted normal semantic-reconnect path (LOBBY: `DISCONNECTED -> CONNECTED` then re-evaluate current admission with no guaranteed slot recovery; RUNNING, if part of the Start-time roster: `DISCONNECTED -> CONNECTED` and process `UserReconnected(user)` before resync; if excluded from the Start-time roster, reconnect must not invent runtime membership or deliver `UserReconnected`).

V1 does not persist physical-disconnect-start timestamps or remaining transport-grace duration, so a restarted Coordinator may restart the full configured grace duration for a durably `CONNECTED` actor with no binding - the same simplicity tradeoff already accepted for Game Language timer obligations (GAME-ADR-0008), applied to a distinct mechanism; no `physical_disconnected_at`, durable grace deadline, or remaining-grace persistence is added.

Recovery grace must never revive a Session whose authoritative lifecycle deadline already passed: if `now >= lobby_expires_at` the lobby is already semantically expired (GAME-ADR-0004) and a fresh recovery grace must not preserve/revive it; if `now >= activity_expires_at` the Session is already semantically expired under the accepted inactivity model (GAME-ADR-0014) and gets no fresh opportunity merely because Coordinator restarted. Authoritative Session lifecycle deadlines take precedence over ephemeral Coordinator recovery grace. Merely reconstructing infrastructure mechanisms after restart (scanning RUNNING Sessions, loading Snapshots, discovering active TimerObligations, rescheduling physical timers, discovering durably `CONNECTED` actors, starting recovery graces, rebuilding Coordinator maps) does not by itself renew `activity_expires_at`; only a later real operation, such as an authenticated reconnect, may qualify as meaningful activity under the already-accepted renewal policy (GAME-ADR-0014). Session Runtime gains no process-specific APIs (`RecoverFromProcessCrash`, `TransferSessionOwnership`, `ClaimSession`, `ProcessDied`, `CoordinatorRestarted`); it continues to expose only ordinary domain/runtime operations regardless of which process invokes them - Coordinator reconstruction is entirely an upper-layer concern.

For a RUNNING runtime member, the durable `semantic_presence` transition and its corresponding Game Language lifecycle processing (`UserDisconnected`/`UserReconnected`) must not be allowed to become separated by a process crash. They are processed as one Session transaction, extending the already-accepted RuntimeTurn transactional model (GAME-ADR-0007) to the presence mutation itself: validate/serialize the Session; transition presence; construct and attempt the root signal; if handled, persist the resulting RuntimeTurn/consequences; if valid but unhandled, preserve the already-accepted no-op semantics (GAME-ADR-0011) with no no-op RuntimeTurn; commit the presence transition and any gameplay consequences atomically. If the process crashes before commit, the presence edge did not occur authoritatively and durable state remains at its prior value - Coordinator may later report the semantic disconnect again; if the transaction committed, presence and any Game Language effect are already authoritative together, and no lifecycle edge is lost. This composes with, and does not modify, the already-accepted RuntimeTurn all-or-nothing crash semantics (GAME-ADR-0007, GAME-ADR-0013).

Canonical references: GAME-ADR-0016, `game/README.md` (Session Runtime Process-Agnostic Recovery section, Session Runtime Durable Inactivity Expiration section, Session Runtime Disconnect/Reconnect/Resynchronization Boundary section), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (clarifying prose in the Process-Agnostic Recovery section; no schema/ER diagram change).

## Accepted Session Runtime Failure Classification And Fatal Diagnostic Persistence

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

Session Runtime distinguishes four failure classes: (A) expected operation/runtime rejection (`ErrSignalRejected`/`ErrInputRejected`, and an unhandled `UserDisconnected`/`UserReconnected`) - no Snapshot mutation, no RuntimeTurn, Session stays RUNNING; (B) deterministic runtime/game execution failure (non-rejection `ExecutionError`: invariant violation, division by zero, occupied slot, invalid quorum/runtime operation, or a safety-limit violation such as execution budget/loop limit/workflow depth) - the attempted Turn does not commit, the previous Turn/Snapshot remains authoritative, and the Session terminalizes as `RUNTIME_EXECUTION_FAILED`; V1 treats safety-limit failures as fatal rather than endlessly retryable; (C) durable runtime-state invalidity (corrupt/unreadable Snapshot, Snapshot incompatible with its pinned Program, impossible/missing durable runtime reference) - terminalizes as `RUNTIME_STATE_INVALID`, kept distinct from (B); (D) transient infrastructure failure (Postgres unavailability, deadlock/serialization retry, network/dependency failure, timeout, process crash where the transaction did not commit) - does not terminalize the Session; last committed state remains authoritative and normal retry semantics apply.

A fatal (B/C) attempted execution is never persisted as a `session_runtime_turns` row; `current_turn_id` remains at the last valid committed Turn, with no fabricated Turn representing the failure. A new conceptual `session_runtime_failures` entity (separate from RuntimeTurn, owns no authoritative Snapshot) holds `id`/`session_id`/`failure_kind` (`RUNTIME_EXECUTION`|`RUNTIME_STATE_INVALID`)/`error_code`/`error_message`/`base_turn_id`/`attempted_sequence`/`failed_step_index`/`source_kind`/`source_interaction_id`/`source_timer_obligation_id`/`actor_id`/`diagnostic_payload`/`created_at`. `attempted_sequence` and `failed_step_index` are diagnostic-only and never consume the authoritative sequence or represent committed state; `diagnostic_payload` is a versioned, not-yet-frozen internal structure whose captured traces (including from successful in-memory Steps before the fatal Step) never become authoritative gameplay history.

For (B)/(C), Session Runtime performs atomic fatal materialization in one transaction - attempt execution, encounter the fatal failure, persist no attempted Turn/Steps, persist the `SessionRuntimeFailure`, set `phase = TERMINAL` + internal `terminal_reason` + `terminal_at`, commit - rather than a two-step rollback-then-terminalize design, so a known-fatal failure can never durably leave the Session RUNNING. If infrastructure fails before that transaction commits, the prior state remains authoritative and no fatal terminalization/`SessionRuntimeFailure` is considered to have happened. Expected rejections and transient infrastructure failures never create a `session_runtime_failures` row.

`RUNTIME_EXECUTION_FAILED` and `RUNTIME_STATE_INVALID` remain distinct internal terminal reasons but both project publicly as a generic `INTERNAL_ERROR` category (naming not frozen); clients never receive raw technical detail (invariant names, division-by-zero, snapshot-mismatch specifics, engine paths/slots, stack traces, `diagnostic_payload`, raw `ExecutionErrorCode`, internal DB/runtime identifiers), and a valid user input that triggers a downstream authored-script failure is never reported back as input invalidity. Live terminal notification happens only after durable `Session TERMINAL` + `SessionRuntimeFailure` commit; disconnected clients recover the same terminal truth through the already-accepted resync capability (GAME-ADR-0010).

Failed Sessions remain normal terminal Sessions for archival (GAME-ADR-0009), but `session_runtime_failures` is explicitly excluded from the automatic hard-delete-after-archival policy - lightweight failure metadata stays relationally queryable (failure counts by error code/game definition) after heavy runtime-history archival, since a Session normally produces at most one such record; large diagnostic-payload compaction strategy remains a later, undesigned optimization.

Canonical references: GAME-ADR-0017, `game/README.md` (Session Runtime Failure Classification And Diagnostic Persistence section, and the archival-exception sentence in Session Runtime History Archival Direction), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (Session Runtime Failure Diagnostics section and archival-exception paragraph).

## Accepted Session RUNNING Mutation Serialization

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

The per-Session serialization guarantee GAME-ADR-0004 already accepts for LOBBY is extended to RUNNING: for one Session, every mutation capable of changing authoritative RUNNING-phase runtime state executes serially, and there must never be two RuntimeTurns concurrently executing against the same authoritative Session state. V1 imposes this through a database-backed pessimistic locking strategy inside Session Runtime's own transaction boundary - the same kind of mechanism GAME-ADR-0004 already assumes for LOBBY - explicitly rejecting process ownership, sticky routing, an in-memory actor, Redis/distributed locks, and Coordinator ownership of correctness as V1 RUNNING-serialization mechanisms. The exact SQL lock anchor/statement remains an implementation-planning detail.

A runtime mutation must reload `session_runtime_state.current_turn_id` and its Snapshot after obtaining serialization, never executing against a Snapshot read before the lock was acquired. Authoritative ordering of concurrent causes is defined purely by which cause wins serialization and commits first (worked example: Turn 40 current; Q5 wins serialization, processes from 40, commits 41; T1 then wins, processes from 41, commits 42) - never by wall-clock/Coordinator-arrival timestamps, process identity, or socket/transport timing. Read-only operations such as resync do not themselves create RuntimeTurns and are not required to participate in this mutation-serialization boundary, though the exact read-isolation implementation is not frozen here.

Canonical references: GAME-ADR-0018, `game/README.md` (new Session Runtime RUNNING Mutation Serialization section), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md`.

## Accepted RuntimeTurn Execution Bound And Terminal Cleanup

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

Session Runtime imposes a V1 Session-level limit, `MAX_STEPS_PER_RUNTIME_TURN = 20` (code/configuration-defined, adjustable, not authored per game, not a durable per-Session setting), on the total `engine.Step` invocations one RuntimeTurn may execute while draining `InternalSignals` to quiescence - counting the initial externally-caused `Step` plus every subsequent internal-signal-triggered `Step` in the same Turn - distinct from `engine.Limits`, which bounds work inside one `Step` call only. This bound also applies to Start's own initial signal/internal-signal chain. Exceeding it is a deterministic fatal failure: no partial RuntimeTurn/gameplay consequence is persisted, `current_turn_id` stays at the last committed Turn, and the Session terminalizes via the already-accepted `RUNTIME_EXECUTION_FAILED` class (GAME-ADR-0017) and its atomic fatal-materialization transaction, carrying a stable diagnostic code equivalent to `runtime_turn_step_limit_exceeded` in `session_runtime_failures.error_code` so operators can query/identify Sessions terminalized specifically for this cause - never exposed to players.

Once a Session becomes `TERMINAL`, for any terminal cause, it must not retain any `ACTIVE` `session_interactions`/`session_timer_obligations` row. Terminalization atomically closes/cancels every still-`ACTIVE` such row in the same transaction that materializes `TERMINAL` - pure Session lifecycle cleanup: no Game Language execution, no RuntimeTurn, no fabricated interaction response/timer expiration, no Snapshot mutation. When this closure is not itself produced by a committed RuntimeTurn, `closed_by_turn_id = NULL` and a closure reason equivalent to `SESSION_TERMINATED` records why the row stopped being active, without duplicating the Session's own `terminal_reason` onto every closed row (that remains discoverable through Session lifecycle/failure metadata). Existing state vocabularies are reused (interactions `ACTIVE -> terminally closed/cancelled`; timers `ACTIVE -> CANCELLED`); the invariant matters more than exact enum naming.

`session_runtime_failures.base_turn_id` is nullable, refining (not superseding) GAME-ADR-0017's original "always required" description: null means the fatal failure occurred before any RuntimeTurn ever committed for the Session, including a fatal failure during Start's own initialization chain; `attempted_sequence = 1` remains valid diagnostic annotation in that case, still not consuming an authoritative sequence. Start's initialization/internal-signal chain is itself subject to `MAX_STEPS_PER_RUNTIME_TURN`; a deterministic failure there (including Step-limit overflow) terminalizes the Session directly from `LOBBY` to `TERMINAL` (`RUNTIME_EXECUTION_FAILED`, `base_turn_id = NULL`, `started_at` left at its canonical null value, no Turn 1, no fabricated Turn 0, no dummy Snapshot), distinct from transient infrastructure failure before commit, which leaves the Session durably `LOBBY` and retryable. A Session is durably `RUNNING` only once Start's first RuntimeTurn/`current_turn_id`, `phase = RUNNING`, `started_at`, and JoinCode revocation all commit together in one transaction.

Canonical references: GAME-ADR-0019, `game/README.md` (Turn And Persistence Model's Step-bound paragraph; Failure Classification's Step-limit-exceeded case and new Terminal Cleanup subsection; Lobby Lifecycle Contract's Start description), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (Terminal Cleanup: Closure Provenance subsection; `session_runtime_failures.base_turn_id` nullability).

## Accepted Session Runtime Post-Commit Client Delivery Semantics

Status: HUMAN-APPROVED and canonically promoted; not yet implemented.

Durable Session commit is the authoritative correctness boundary: `runtime execution -> durable Session commit -> best-effort live delivery`. A later failure to deliver Coordinator/WebSocket/client outputs never rolls back a RuntimeTurn, undoes a Snapshot, reopens a closed interaction, undoes a timer obligation, or undoes terminalization. V1 has no generic durable Coordinator/WebSocket delivery outbox (no `session_delivery_outbox`, `delivery_attempts`, persistent connection delivery offsets, or per-client ACK tables) unless a future, separately-approved requirement introduces one. Anything needed for authoritative correctness/recovery (RuntimeTurn/Snapshot, lifecycle state, Interactions, TimerObligations, semantic presence, terminal state, fatal diagnostics) must already be durable before delivery is attempted - live delivery communicates durable truth, it does not create it.

Reconnect/resync (GAME-ADR-0010) reconstructs *current* authoritative player-facing truth, not a replay of every missed live message or intermediate presentation transition (worked example: Turn 42 commits and opens interaction Q17; the process dies before Q17 reaches the client; after reconnect, Q17 is still an `ACTIVE` durable `SessionInteraction` and appears in the current resync projection - no outbox required). Physical timer-scheduling failure after a Turn commits does not roll back the Turn; Coordinator recovery/reconciliation rediscovers and reschedules the durable obligation instead (GAME-ADR-0008, GAME-ADR-0013). Purely ephemeral presentation effects (animation, sound, confetti) may be lost on delivery/process failure with no guaranteed replay. This best-effort rule covers Coordinator/client/presentation delivery only - it must not silently become the reliability contract for a future Game Language output with an externally irreversible side effect (payment, external API mutation, durable notification, cross-domain command); such a capability requires its own explicitly designed delivery/idempotency/retry semantics as a separate future decision.

Canonical references: GAME-ADR-0020, `game/README.md` (new Session Runtime Post-Commit Client Delivery Semantics section; Live Session Coordinator boundary description), `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (No Durable Client-Delivery Outbox section).

## Deferred Design Topics

Authored Game Language disconnect/reconnect semantics, process-agnostic recovery/RuntimeTurn crash semantics, durable RUNNING inactivity expiration, durable SessionActor semantic presence/phase-dependent lobby-to-runtime membership, semantic presence recovery after total Coordinator/process loss, runtime/internal failure classification and fatal diagnostic persistence, RUNNING-phase mutation serialization, the RuntimeTurn Step-chain execution bound, terminal cleanup of outstanding interactions/timer obligations, and post-commit client delivery semantics are now CLOSED at architecture level (see the sections above) - these milestones are done, not deferred. Session Runtime V1 Architecture Discussion is itself now CLOSED (see Architecture Discussion Closure below).

Remaining topics are implementation-planning/configuration-level, not architecture blockers, per the human-approved closure determination: exact platform abuse/resource limits and per-user/session rate boundaries, maximum pending interactions/timers per Session, corrupted/invalid Game Definition handling before Start, exhaustive `source_kind`/interaction-kind enums, host transfer, the exact public DTO/HTTP/WebSocket status-code contract for `INTERNAL_ERROR`, the final JSON archive schema and GCS implementation, the final idempotency JSON canonicalization/comparison algorithm, the concrete `KeyedTimerSlot<Key>` compiler/engine design and `engine_key` serialized representation, the exact renewal-triggering-operation enumeration and `inactivity_ttl` configuration surface, the exact SQL types/uniqueness constraint/large-payload retention strategy for `session_runtime_failures`, the exact SQL lock anchor/statement for per-Session serialization, the concrete `MAX_STEPS_PER_RUNTIME_TURN`/`closure_reason`/`runtime_turn_step_limit_exceeded`-equivalent constant/enum/code surface, and the exact table/index definitions. Genuinely out-of-scope-for-V1 topics remain: mutable global display/profile ownership, Identity reconciliation/merge/alias semantics, Session Configuration/arbitrary external game-specific root params, post-Start late admission/dynamic runtime membership, and the post-launch client-state-loss re-entry idea recorded non-authoritatively in `docs/product/IDEAS.md` (not designed, not V1, not approved implementation).

## Architecture Discussion Closure

Status: HUMAN-APPROVED, 2026-09-08. The human explicitly approved the conclusion: "Session Runtime V1 Architecture Discussion: COMPLETE." No currently known unresolved issue materially blocks moving into implementation planning with respect to: domain/capability ownership; Session vs. Coordinator boundary; Identity/User boundary; lifecycle; lobby admission; Start roster; RuntimeTurn; concurrency/DB locking; persistence; interactions; timers and keyed timers; disconnect/reconnect; semantic presence; resync; process-loss recovery; inactivity expiration; archival direction; runtime failure taxonomy; fatal diagnostics; terminal cleanup; RuntimeTurn execution bound; post-commit delivery. Remaining items (exact inactivity TTL, connection grace duration, rate-limit numbers, engine budget values other than the accepted `MAX_STEPS_PER_RUNTIME_TURN = 20`, SQL types/indexes, HTTP statuses, WebSocket DTO shapes, final archive JSON layout, GCS implementation details) are implementation-planning/local-design decisions subject to normal escalation rules, not architecture blockers. No new material contradiction was found during this checkpoint's checkout inspection beyond the drift already tracked below.

## Implementation Planning

The initiative is in Initiative Implementation Planning (see `docs/ai/protocols/CONVERSATIONAL_ORCHESTRATOR.md`). The full decomposition - implementation goal, actual current-code drift, the human-approved 10-slice order with dependencies, deferred/out-of-scope items - is persisted in `docs/ai/workspaces/active/session-runtime-v1/PLAN.md`, which is temporary/non-canonical/non-authoritative and must not be treated as accepted architecture. **Slice 1 - Session Lobby Foundation has been materialized into Feature Development as `docs/work/active/WORK-0001-session-lobby-foundation.md`, and is now READY** - this workspace is therefore currently operating as that Feature Development sub-process (see the resume header above); implementation may begin in the next handoff/session. Slice 2 (Start + First RuntimeTurn) is the next candidate once WORK-0001 reaches DONE, per the just-in-time WORK model.

## Mandatory Standard-Compliance Migration

Status: RECORDED 2026-09-09, **COMPLETED 2026-09-11**. `docs/engineering/standards/domain-logic-placement.md` and `docs/engineering/standards/repositories.md` were clarified (human-approved Engineering Standard change; see `docs/ai/workspaces/active/behavior-locality-and-workflow-organization/AI_CONTEXT.md`) after Slice 1's implementation exposed real ambiguity the prior standard wording did not resolve. This was a targeted migration requirement on the current Session Runtime implementation, not a judgment that the Codebase Agent violated a rule that existed clearly at the time.

WORK-0001's implementation was migrated to at least the required minimum (see `docs/work/active/WORK-0001-session-lobby-foundation.md` -> "Standard-Compliance Migration Record" for the full record):

- moved `CreateSession`/`JoinSession`/`LeaveSession` (previously `game/session/usecases/{createsession,joinsession,leavesession}/`) into an explicitly discoverable Session-lifecycle workflow organization, now `game/session/workflows/sessionlifecycle/{createsession,joinsession,leavesession}/`, preserving focused per-operation components - no giant lifecycle service was introduced;
- removed the horizontal `game/session/internal/actors` persistence package (SessionActor/Participant Find/Create/Reactivate/Deactivate/CountActive), which was exactly the shared entity-CRUD pattern the clarified `repositories.md -> Sharing Rule` disallows;
- migrated the Actor/Participant persistence Join and Leave each need into behavior-local unexported functions inside each consumer's own `repo.go`, per `domain-logic-placement.md -> Repository Contracts` (small duplication between Join's and Leave's persistence code is intentional, per `repositories.md`'s Sharing Rule - not a defect);
- retained the legitimate shared mechanisms unchanged: `game/session/internal/sessionlock`, `game/session/internal/idempotency`, `game/session/internal/pgerrs` remain exactly the kind of cross-cutting protocol/mechanism the standard says to keep centralized.

No behavior changed (same SQL, same transaction boundaries, same public types/errors); `go build ./...`/`go vet ./...` are clean and all service-level tests pass (`go test -count=1 ./game/...`). This closes the standard-compliance gate on Slice 2. Two gates remain before WORK-0001 can reach DONE and Slice 2 can begin: real-Postgres integration/concurrency test verification, and independent implementation review per `docs/ai/protocols/IMPLEMENTATION_REVIEW.md`.

## Current Implementation Facts And Drift To Carry Forward

See `PLAN.md`'s "Actual Implementation Drift Found" section for the full, current inspection (existing Session Runtime scaffolding does not compile, contradicts accepted architecture, and has none of the accepted persistence-model tables; Game Language engine and the Game Management read capability are already implemented and reusable; no Coordinator/WebSocket code exists anywhere, not even a placeholder; Identity has no implementation, which Slice 1 must account for). `game/CURRENT_STATE.md` and `game/docs/DATA_MODEL.md` were not modified to pretend implementation exists - both still describe the pre-existing schema; `game/CURRENT_STATE.md` previously reported no known drift, and this workspace/PLAN.md record the drift without editing current-state docs, since implementation has not happened yet.

**New drift as of the 2026-09-16 design revision**: the actual Go implementation under `game/session/workflows/sessionlifecycle/{createsession,joinsession,leavesession}/` was left completely untouched by this handoff (documentation-only), so it still matches what `game/CURRENT_STATE.md`/`game/docs/FLOWS.md` describe - those current-state docs remain accurate to actual code and were correctly not touched. What changed is the *approved target* in `WORK-0001-session-lobby-foundation.md`, which now requires a materially different implementation boundary (one `Manager` workflow controller instead of three sibling packages; explicit-parameter methods instead of `leaveParams`-style structs; explicit transaction ownership instead of repository-owned transactions; persistence-oriented repository naming instead of `repo.leaveSession(...)`-style methods that decide business policy; expiration/admission policy moved out of `sessionlock.MaterializeExpirationIfDue` and repository methods into the Manager; token-aware Join idempotency per GAME-ADR-0021 instead of the previous state-based "repeated active Join is idempotent" reading). This is a known, expected implementation/approved-design gap - not silently resolved by this handoff - to be closed by the next implementation pass against the revised WORK-0001.

## Explicitly Not Done

- No WORK was created; nothing is marked READY.
- No production code, tests, migrations, authentication code, WebSocket/Coordinator handlers, or Session Runtime implementation were changed.
- No compiler/engine/program Go code was changed; the pre-existing empty-schema `UserDisconnected` placeholder, the missing `UserReconnected`, and the missing `KeyedTimerSlot` capability were left exactly as found.
- No current-state docs were modified to pretend implementation exists.
- No GCS integration, archival worker, Reaper, recovery worker, DB locking mechanism, `MAX_STEPS_PER_RUNTIME_TURN` constant, terminal-cleanup transaction, or `session_runtime_failures` implementation was added anywhere - every architecture decision through GAME-ADR-0020 remains design-only.
- The post-launch "Session re-entry after complete client-state loss" idea was recorded in `docs/product/IDEAS.md` only as a non-authoritative idea; no UX/API/identity mechanism, guest-recovery credential design, or WORK was created for it, and guest identity semantics were not changed.
- No `session_runtime_failures` migration/code, no `SessionRuntimeFailure` implementation, no engine/compiler change, no Coordinator behavior change, no observability pipeline, no archival worker, and no WORK were created for the runtime/internal failure semantics milestone - the four-class taxonomy, RuntimeTurn failure boundary, diagnostic entity, atomic fatal materialization, and public/internal error separation were accepted as design only.
- Concrete execution-budget/loop-limit/workflow-depth values, the exhaustive `source_kind`/interaction-kind enums, and the exact public DTO/HTTP/WebSocket status-code contract for `INTERNAL_ERROR` were not designed - deferred per GAME-ADR-0017.
- No DB locking mechanism/SQL statement, no `MAX_STEPS_PER_RUNTIME_TURN` constant, no Step-counting code, no terminal-cleanup transaction, no `closure_reason` migration/column, no `runtime_turn_step_limit_exceeded`-equivalent error code, no engine/compiler change, and no WORK were created for the RUNNING-serialization or RuntimeTurn-execution-bound/terminal-cleanup milestones - GAME-ADR-0018 and GAME-ADR-0019 were accepted as design only.
- The final Operational Lifecycle completeness review (post-commit delivery) was performed in the prior checkpoint and found no material architecture blocker; Architecture Discussion was explicitly closed there.
- A prior checkpoint synchronized the human-approved slice sequencing into `PLAN.md`, repaired stale contradictory resume-state text in this file, and created exactly one DRAFT WORK (`WORK-0001-session-lobby-foundation.md`) for Slice 1.
- This checkpoint applied the human's implementation-design corrections to WORK-0001 (pinned Game Definition immutability, a minimum new Game Management read capability, the `(user_uuid, operation, idempotency_key)` idempotency namespace, concurrent-Create claim-first correctness, completed-vs-transient-failure semantics, the Session/HostActor insertion cycle, and corresponding new/updated acceptance criteria - see Last durable checkpoint above), then marked WORK-0001 **READY**. `PLAN.md` and this file were synchronized to reflect READY status everywhere WORK-0001 is referenced. No production Go code, tests, or SQL migrations were written or changed; the existing broken/architecture-contradicting `game/session/...` scaffolding remains untouched pending actual implementation. No Game Management Go code was changed - only WORK-0001's design now specifies the minimum new read capability Game Management implementation must add. No WORK exists for Slices 2-10; per the just-in-time WORK model, they remain unmaterialized until WORK-0001 reaches DONE and the plan is reevaluated.
