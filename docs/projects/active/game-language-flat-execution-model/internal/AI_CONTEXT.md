Process: Project (`docs/projects/active/game-language-flat-execution-model/`)
Topic: Game Language flat execution model (removal of Child Workflow/Task Group; keyed interaction slots; engine-owned interaction addressing)
Current stage: WORK-0024 DONE (2026-09-24). WORK-0025 (Keyed Question, Ask Group, and Timer Slots) drafted, both Blockers human-resolved, promoted READY, fully implemented, independently reviewed (two passes), fixed, and closed DONE — all the same day. See `../works/WORK-0025-keyed-interaction-slots.md`'s Completion Record for full history. WORK-0026 is next to draft for real; WORK-0027 remains PLANNED.
Current execution surface: CODEBASE AGENT (implementation + review-and-fix, combined task explicitly requested by the human)
Parent process: this Project itself.
Related durable artifacts: `../PROJECT.md` (Work table/Current Work/a new "Tracked Follow-Ups" section updated to reflect WORK-0025 DONE), `../works/WORK-0025-keyed-interaction-slots.md` (full design + Implementation Report + two independent review passes), `../works/WORK-0024-remove-child-workflow-and-task-group.md` (DONE, full history), `HUMAN_REVIEW.md` (this directory — RESOLVED, kept as historical record), `game/docs/decisions/GAME-ADR-0026-...md`, `game/docs/decisions/GAME-ADR-0012-...md` (both gained an "Implemented by" cross-reference).
Blocked by: nothing. WORK-0025 is DONE.
Next action: per this Project's just-in-time drafting practice, draft WORK-0026 (Engine-Owned Interaction Addressing) for real once work resumes on this Project.
Last durable checkpoint: this pass (2026-09-24) — WORK-0025 fully implemented (program/codec/gameservice/engine/compiler/runtime layers, 50 new test functions across dedicated new test files, documentation synchronized across `program`/`engine` READMEs, `LOGICAL_CONTRACT.md`, `DEFINITION.md`, `game/README.md`, `SESSION_RUNTIME_PERSISTENCE_MODEL.md`, and both relevant GAME-ADRs), independently reviewed twice (first pass found and this pass fixed a HIGH-severity Snapshot-codec bug plus several lower-severity findings; second pass re-verified all fixes directly and returned APPROVED), and closed to DONE. One pre-existing `engineservice.Step` characteristic was discovered (Control does not see the same transition's own global-state mutations) and recorded, not fixed - out of this WORK's scope. One pre-existing NON_BLOCKING doc-comment drift in `program/ask_group.go` (unrelated to this WORK) was found during the second review and recorded in `../PROJECT.md`'s new "Tracked Follow-Ups" section, not fixed. `go build`/`go vet`/full test suite (including real Postgres) clean except the already-known out-of-scope `getgame` defect.
Last updated: 2026-09-24

# Resume Context

## What happened this pass

WORK-0024 (Remove Child Workflow and Task Group) reached DONE in an earlier pass this same day, after three independent review cycles (full history preserved in WORK-0024's own Completion Record, not repeated here).

This pass then drafted, human-resolved (Blocker 1: Timer's keyed slot included, finally implementing GAME-ADR-0012; Blocker 2: Presentation's keyed capability explicitly deferred, not approved), promoted to READY, and fully implemented WORK-0025 (Keyed Question, Ask Group, and Timer Slots) across every layer — `program`, `program/internal/codec`, `program/gameservice`, `engine`, `engine/internal/compiler`, `engine/internal/runtime`, `engine/internal/codec` — with documentation synchronized throughout (`program`/`engine` READMEs, `DEFINITION.md`, `LOGICAL_CONTRACT.md`, `IMPLEMENTATION.md`, `game/README.md`, `SESSION_RUNTIME_PERSISTENCE_MODEL.md`, both relevant GAME-ADRs).

A fresh independent review was then performed per `docs/ai/protocols/IMPLEMENTATION_REVIEW.md` against the self-reported-ready implementation, reasoning from primary evidence rather than trusting the self-report. It found:
- One HIGH-severity REQUIRED_FIX: `engine/internal/codec/instance.go`'s Snapshot codec never encoded/decoded the 3 new keyed-slot `WorkflowInstance` fields, silently dropping all keyed-slot state across persistence (encode-then-decode would lose every pending keyed question/ask-group/timer).
- Several lower-severity REQUIRED_FIX findings: missing presentation-mounting test coverage, weak atomicity test coverage, stale/overclaiming doc comments and tracking text (`PROJECT.md`, this file).
- One DECISION_REQUIRED finding: whether AskGroup should also gain per-key presentation mounting, since the WORK's own Scope/AC text had overclaimed this as implemented when it was not.

Every REQUIRED_FIX finding was applied in this same session: the critical codec bug was fixed with full wire encode/decode for all 3 keyed families, plus a comprehensive round-trip test (`engineservice/keyed_codec_test.go`) that also proves the restored state is genuinely usable for continued execution, not just byte-identical; a presentation-mounting test and a genuine multi-operation atomicity test were added; stale doc comments and tracking text were corrected throughout. The one DECISION_REQUIRED finding was resolved without escalating to the human, reasoning that WORK-0025's own already-approved Constraint ("no change to ordinary slot semantics") already settles it: since ordinary (non-keyed) AskGroup never mounts presentations either, correcting the overclaiming text to match actually-approved behavior is a documentation fix, not a new decision — implementing new AskGroup presentation-mounting behavior would itself be the unauthorized scope expansion. This self-resolution still needs to be flagged to the human for visibility/override in the final summary, not treated as silently final.

Also discovered, recorded, and deliberately NOT fixed (out of WORK-0025's scope): a pre-existing `engineservice.Step` characteristic where `WorkflowControl` evaluates against the transition's scope as captured before that same transition's own Operations mutate global/local state — this contradicts `program/control.go`'s own documented contract that Control should observe such mutations. This is real canonical-vs-implementation drift, not caused by WORK-0025.

## What happened after that (closure)

The factual-error correction (false "no generics" claim; stale test count) was applied to the Completion Record. A second, genuinely fresh independent review was then run specifically to re-verify the fix pass given the codec bug's severity (mirroring WORK-0024's own three-pass precedent) — it independently verified every claimed fix against the actual current code/docs, re-ran the full verification suite directly, and returned APPROVED with one NON_BLOCKING finding (the pre-existing, unrelated `program/ask_group.go` doc drift, recorded in `../PROJECT.md`'s "Tracked Follow-Ups" section). WORK-0025 was then closed: Status set to DONE, Completion Record finalized with an "Independent Re-Review (2026-09-24)" section, `../PROJECT.md` and this file updated.

## What is NOT done

- WORK-0026/0027 remain PLANNED, not yet drafted for real — per this Project's own just-in-time drafting practice, each is drafted only once the WORK immediately before it is READY/DONE.
- The `program/ask_group.go` NON_BLOCKING doc drift (tracked in `../PROJECT.md`) has not been fixed — deliberately, it is outside WORK-0025's scope.
- The Snapshot-codec bug (found and fixed) and the self-resolved DECISION_REQUIRED finding (AskGroup presentation mounting, independently re-confirmed by the second review) have not yet been communicated to the human in a final summary within this conversation.
