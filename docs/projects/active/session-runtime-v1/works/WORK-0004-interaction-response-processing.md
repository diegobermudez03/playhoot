# WORK-0004: Interaction Response Processing

Status: DONE
Created: 2026-09-19
Last status change: 2026-09-19

Related decisions:
- GAME-ADR-0007 (RuntimeTurn/Interaction persistence model)
- GAME-ADR-0018 (RUNNING mutation serialization)
- GAME-ADR-0019 (RuntimeTurn Step bound, terminal cleanup)
- GAME-ADR-0017 (failure classification)
- GAME-ADR-0022 (business declines as workflow outcomes)

Canonical context:
- `game/README.md` (Session Runtime Turn And Persistence Model, RUNNING Mutation Serialization, RuntimeTurn Execution Bound And Terminal Cleanup sections)
- `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md` (`session_interactions` shape, Turn/Interaction relationships)
- `game/language/v1/engine/README.md` (Step contract, Outputs table, Signal kinds)
- `docs/projects/active/session-runtime-v1/PROJECT.md` (Slice 3)
- `game/session/workflows/sessionlifecycle/step_start.go` (existing RuntimeTurn execution/persistence pattern this WORK extends)

## Outcome

A player can answer an open interaction (a `Question` or an `AskGroup` question) against a `RUNNING` Session, entirely at the Go-API level (no transport yet - Slice 4 exposes this live). Session Runtime obtains RUNNING-phase per-Session serialization (GAME-ADR-0018), reloads current authoritative state, applies the accepted response through the Game Language engine, persists the resulting RuntimeTurn, and durably resolves the `session_interactions` row the response answered.

This WORK also closes a gap discovered while designing it: no Turn-producing path today persists `session_interactions` at all - `step_start.go`'s `drainRuntimeTurn` discards `commit.Outputs` entirely, so even a game whose root workflow opens a question immediately at Start would leave that interaction with no durable record for anyone to ever answer. This WORK is therefore also where `OpenQuestionOutput`/`CloseQuestionOutput` capture is introduced, retrofitted onto Start and shared with the new interaction-answering step.

## Context

Slice 1 (WORK-0001) and Slice 2 (WORK-0003) are DONE: `sessionlifecycle.Manager` exposes `Create`/`Join`/`Leave`/`Start`; `Start` executes the engine's first RuntimeTurn and commits `phase = RUNNING`, `sessions.current_turn_id`, per GAME-ADR-0023. No RUNNING-phase mutation exists yet - `sessions.current_turn_id` is written once by Start and never read/advanced again. No `session_interactions` table exists yet (not yet migrated). GAME-ADR-0018 (RUNNING serialization) and GAME-ADR-0019 (Step bound, terminal cleanup) are accepted architecture but have zero implementation surface outside what Slice 2 already needed for Start's own initialization chain.

The engine (`game/language/v1/engine`) exposes two ways a workflow can request player input - `OpenQuestionOperation` (single recipient) and `OpenAskGroupOperation` (many recipients, with a completion policy) - both surfacing as one `OpenQuestionOutput` per recipient when opened, and closing via `CloseQuestionOutput`. A submitted answer re-enters the engine as `engine.Signal{Kind: SignalKindQuestionAnswered, ...}` or `SignalKind AskGroupAnswered, ...}` (same `Slot`/`Respondent`/`Answer` shape, distinguished only by `Kind`) through `engineservice.Step`. `OpenQuestionOutput` itself carries no discriminator saying which of the two originated it.

Per PLAN.md, Slice 3 is deliberately the simplest RUNNING-phase mutation (no physical scheduling, unlike timers) specifically to validate GAME-ADR-0018's serialization mechanism with the fewest moving parts, and to complete the minimal create->join->start->answer vertical slice Slice 4's thin Coordinator will later expose live.

## Scope

### In Scope

- A new durable `session_interactions` table (migration), per the accepted shape in `SESSION_RUNTIME_PERSISTENCE_MODEL.md`.
- Capturing `OpenQuestionOutput`/`CloseQuestionOutput` from any committed RuntimeTurn's `Commit.Outputs` and persisting/closing the corresponding `session_interactions` row - applied to **both** the new interaction-answering step and a retrofit of `step_start.go` (see Blockers).
- A new `AnswerInteraction` step directly on the existing `sessionlifecycle.Manager` (alongside `Create`/`Join`/`Leave`/`Start`, not a new sibling workflow package - see Blockers) that: resolves an interaction by its public UUID, obtains RUNNING-phase per-Session serialization (extending the existing `sessionlock` mechanism to a `RUNNING` Session - no new locking mechanism), reloads `sessions.current_turn_id` and its Snapshot, validates the interaction is still `ACTIVE` and the caller is its recipient, constructs the correct `engine.Signal` for the stored interaction `kind`, drains the resulting RuntimeTurn to quiescence under the existing `MAX_STEPS_PER_RUNTIME_TURN = 20` bound, and atomically persists the new RuntimeTurn/Steps, advances `sessions.current_turn_id`, and resolves the answered `session_interactions` row (`response_payload`, `state`, `closed_by_turn_id`).
- Expected-rejection handling: an engine `ErrSignalRejected`/`ErrInputRejected` (stale/duplicate/unauthorized/invalid answer) is an ordinary declined outcome - no RuntimeTurn, no Snapshot mutation, Session stays `RUNNING` (GAME-ADR-0017 class A).
- Fatal-failure handling reusing Start's existing pattern: a deterministic `ExecutionError` terminalizes the Session (`RUNTIME_EXECUTION_FAILED`) via the same atomic fatal-materialization transaction shape Start already uses, extended with the narrow terminal-cleanup this slice's own path needs (see Blockers) - not the full Slice 6 diagnostics entity.
- Extracting the pure Step-draining/bound execution logic (`step_start.go`'s `drainRuntimeTurn`) into a shared mechanism scoped to the `sessionlifecycle` workflow itself, now that a second, independent caller genuinely needs it (see Blockers) - never a session-level shared package.

### Out of Scope

- Timer obligations/expiration (Slice 5).
- Disconnect/reconnect/semantic presence (Slice 7).
- The thin live Coordinator/WebSocket transport and any wire/DTO format for the interaction answer payload (Slice 4) - this WORK's public method accepts an already-typed `engine.Value`, exactly as `Start` already accepts typed `engine.Value`/`engine.UserID` rather than a raw wire payload.
- The general-purpose terminal-cleanup sweep and the `session_runtime_failures` diagnostic entity (Slice 6) beyond the narrow path-scoped cleanup this slice's own fatal path requires (see Blockers).
- `AskGroup`-specific aggregate semantics beyond routing one individual accepted answer (per-recipient completion policy evaluation, `AskGroupCompletedSignalSource` join handling) - already the engine's own responsibility inside one `Step` call; this WORK does not add any Session-Runtime-side aggregate bookkeeping beyond the one `session_interactions` row per recipient every `OpenQuestionOutput` already implies.
- Any change to `Create`/`Join`/`Leave` behavior.

## Approved Design

Beyond canonical context, this WORK settles:

- **RUNNING-phase locking reuses the existing mechanism unchanged.** `game/session/internal/sessionlock.LockByUUID`/`LockByID` already lock a `sessions` row regardless of `phase`; GAME-ADR-0018 requires no new lock primitive, only that a RUNNING-phase mutation use it the same way LOBBY mutations already do, then reload `current_turn_id`/Snapshot after acquiring it (never trusting a pre-lock read).
- **No `session_requests` idempotency record for interaction responses.** Per GAME-ADR-0007's already-accepted "Interaction response semantics": the interaction UUID plus its persisted `state`/`response_payload` is the natural dedup identity. A first valid response resolves the interaction; a retry submitting an equivalent response is idempotent (same outcome, no second engine effect); a conflicting different response against an already-resolved interaction is rejected as a workflow outcome (GAME-ADR-0022 pattern), never a second engine effect.
- **Public method accepts a typed `engine.Value` answer directly**, not a wire payload - decoding a transport-level answer into `engine.Value` against the interaction's recorded question/response type is Coordinator/transport-layer work (Slice 4), consistent with GAME-ADR-0002's boundary and PLAN.md's explicit "Go-API level" framing for Slices 1-3.
- **Respondent authorization**: the caller-supplied `UserUUID` must resolve to the `SessionActorID` recorded as the interaction's `session_actor_id` (the recipient at open time); a mismatch is rejected the same way `engineservice.Step` would reject an unauthorized `Respondent`, without needing to reach the engine at all.

## Constraints and Invariants

- Every RUNNING-phase mutation must acquire per-Session serialization before executing, and must reload current state after acquiring it (GAME-ADR-0018) - never execute against a Snapshot read before the lock.
- One RuntimeTurn's Step-chain execution (initial signal plus every internal-signal-caused Step) must never exceed `MAX_STEPS_PER_RUNTIME_TURN = 20` (GAME-ADR-0019); exceeding it is a fatal `RUNTIME_EXECUTION_FAILED` termination, identical in kind to Start's own existing enforcement.
- A fatal (non-rejection) failure must never leave a partially-committed RuntimeTurn; `current_turn_id` remains at the last committed Turn (GAME-ADR-0017).
- `session_interactions.opened_by_turn_id`/`closed_by_turn_id`/the causing Turn's `source_interaction_id` are Turn-level references only, never Step-level (GAME-ADR-0007).
- Authoritative ordering of concurrent causes is defined purely by which cause wins serialization and commits first - never by arrival/wall-clock timestamps (GAME-ADR-0018).

## Acceptance Criteria

- A `Question`/`AskGroup` interaction opened by any committed RuntimeTurn (including Start's own first Turn) is durably persisted as an `ACTIVE` `session_interactions` row before that Turn's transaction commits.
- A valid accepted-recipient response to an `ACTIVE` interaction commits a new RuntimeTurn, advances `sessions.current_turn_id`, and resolves the interaction (`state`, `response_payload`, `closed_by_turn_id` set to the causing Turn).
- A stale, duplicate, unauthorized-respondent, or type/validation-invalid answer is rejected as an ordinary declined outcome: no RuntimeTurn is created, `current_turn_id` is unchanged, and the interaction remains `ACTIVE`.
- A retried, semantically equivalent response to an already-resolved interaction replays the same outcome without a second engine execution; a conflicting different response to an already-resolved interaction is rejected without engine execution.
- A deterministic engine execution failure (including Step-bound overflow) while processing a response terminalizes the Session (`RUNTIME_EXECUTION_FAILED`) atomically, with no partial RuntimeTurn persisted and `current_turn_id` unchanged.
- Two concurrent responses/causes targeting the same Session never both execute a RuntimeTurn concurrently; exactly one wins serialization and commits first, and the other observes the resulting committed state.
- `go build ./...`, `go vet ./...`, and `go test ./... -count=1` (including real-Postgres repository-integration/concurrency tests) pass, with no new failure introduced beyond the already-recorded out-of-scope `getgame` JSONB-comparison test defect.

## Implementation Freedom

- Exact Go type/method/function names for the new `AnswerInteraction` step and its narrow repository contract, subject to `repositories.md`/`domain-logic-placement.md`/`function-signatures.md`.
- Whether the extracted shared RuntimeTurn-execution logic takes the shape of a plain unexported helper shared across `sessionlifecycle`'s own files, or a workflow-scoped `game/session/workflows/sessionlifecycle/internal/runtimeturn` package - either is acceptable per Blocker 2's resolution, so long as it stays inside the `sessionlifecycle` workflow package tree and never at `game/session/internal/...`.
- Exact SQL for the `session_interactions` migration (column types, indexes), consistent with the existing migration style and the accepted column list in `SESSION_RUNTIME_PERSISTENCE_MODEL.md`.
- Exact mechanism for resolving `session_interactions.kind` from the compiled `engine.Program`'s slot declarations, per Blocker 4's resolution below.
- Test structure/fixtures, following `testing.md` and the existing Slice 1/2 test layout.

## Verification

- `go build ./...`, `go vet ./...`, `gofmt -l` on changed files.
- `go test ./... -count=1` against a real PostgreSQL instance (`TEST_DATABASE_*`) - new repository-integration tests for interaction persistence/resolution, and concurrency tests for two competing causes against the same Session (mirroring `TestManagerStart_Integration`'s concurrency subtest).
- Mocked-collaborator unit tests for the new step's pre-transaction/in-transaction business logic (authorization, stale/duplicate/conflict handling, outcome selection).
- Engine-level tests proving Step-bound enforcement/atomicity reused correctly from the extracted shared package, and `OpenQuestionOutput`/`CloseQuestionOutput` capture correctness, without needing a real Postgres connection - mirroring `TestDrainRuntimeTurn`'s existing style.

## Documentation Impact

### Accepted / Canonical Knowledge

- None expected. `game/README.md`, GAME-ADR-0007/0017/0018/0019/0022, and `SESSION_RUNTIME_PERSISTENCE_MODEL.md` already describe this design; this WORK implements it without changing it - unless Blocker resolution below reveals a genuine gap in accepted architecture, which would route through the appropriate decision process instead of being silently decided here.

### Current-State Documentation After Implementation

- `game/CURRENT_STATE.md` - describe interaction response processing as implemented, and Start's retrofitted interaction-capture behavior.
- `game/docs/DATA_MODEL.md` - add `session_interactions` as a now-implemented table.
- `game/docs/FLOWS.md` - add the interaction-response sequence, and update Start's sequence to show interaction capture.

### Intentionally Unchanged

- `docs/projects/active/session-runtime-v1/works/WORK-0001-session-lobby-foundation.md`, `WORK-0002-rename-game-management-package.md`, `WORK-0003-session-start-first-runtimeturn.md` (completed work, historical).

## Blockers

Status: **RESOLVED (2026-09-19, HUMAN-APPROVED)**. Each item below shaped either this WORK's own durable schema/behavior or a pattern later slices (5, 6, 7) inherit, so none was silently decided within existing accepted architecture without human confirmation. All five are now resolved and this WORK is READY.

1. **Workflow classification: new sibling workflow package, or a step on the existing `sessionlifecycle.Manager`?** Proposed: a new `game/session/workflows/runtimeexecution` package. **Resolution: REJECTED as proposed, resolved with the stated alternative instead** - `AnswerInteraction` is a new step directly on the existing `sessionlifecycle.Manager` (`step_answer_interaction.go`, alongside `step_create.go`/`step_join.go`/`step_leave.go`/`step_start.go`), following the same Manager/narrow-repo-contract/idempotency pattern those steps already use. Whether `TimerExpired` (Slice 5) or disconnect-reconnect (Slice 7) processing also lands on `sessionlifecycle.Manager` or eventually warrants a separate package is explicitly left undecided here - each of those slices decides for itself, just-in-time, when it materializes, the same way Blocker 2 below is itself a just-in-time extraction decision rather than one made preemptively at Slice 2.
2. **Extract the shared RuntimeTurn-execution mechanism now.** Proposed: extract `step_start.go`'s pure `drainRuntimeTurn`/Step-bound logic into `game/session/internal/runtimeturn` (a session-level shared package, sibling to `sessionlock`/`idempotency`/`pgerrs`). **Resolution: REJECTED as proposed at that location.** The extracted mechanism must stay scoped to the `sessionlifecycle` workflow itself, not promoted to session level - this is still workflow execution logic two steps of the *same* workflow now share, not a horizontal capability every Session Runtime workflow needs (unlike `sessionlock`/`idempotency`, which genuinely are). Acceptable shapes: a plain unexported helper shared across `sessionlifecycle`'s own files (e.g. a `runtime_turn.go` alongside `manager.go`), or, if a separate package is warranted, `game/session/workflows/sessionlifecycle/internal/runtimeturn` (sibling to the existing `sessionlifecycle/internal/repo`) - never `game/session/internal/runtimeturn`. The exact choice between the two is Implementation Freedom. `step_start.go` is refactored to call the extracted function; nothing about its behavior changes. Each step still independently persists its own RuntimeTurn/Steps/Interactions via its own local repository, per `repositories.md`'s Sharing Rule (mechanism is shared, entity persistence is not).
3. **Retrofit `step_start.go` to capture interactions opened by Start's own first Turn.** **Resolution: APPROVED as proposed** - in this WORK's scope, so a game whose root workflow opens a question immediately at Start actually produces a durable `session_interactions` row `AnswerInteraction` can resolve.
4. **How `session_interactions.kind` (`QUESTION` vs `ASK_GROUP`) is determined.** **Resolution: APPROVED as proposed** - resolved from the compiled `engine.Program`'s own slot declaration for `(workflow, engine_slot)` at the moment the interaction is captured, and persisted on the `session_interactions` row so response processing never needs to re-derive it. Still not deeply verified against `internal/compiler`'s actual shape - if implementation finds the compiled `Program` doesn't actually expose this cleanly, that is new information routed back through this WORK rather than silently worked around.
5. **Narrow terminal-cleanup scope for this slice's own fatal path.** **Resolution: APPROVED as proposed, now** - this WORK implements the narrow case: when this slice's own fatal-termination transaction commits, it atomically closes every currently-`ACTIVE` `session_interactions` row for that Session in the same transaction (`closed_by_turn_id = NULL`, a `SESSION_TERMINATED`-equivalent closure reason), leaving the fully general sweep (covering future entities like timers) to Slice 6.

Local implementation choices (exact SQL/column types for the `session_interactions` migration, test structure/fixtures) remain Implementation Freedom and were never blockers.

## Completion Record

Not yet DONE. Status transitioned DRAFT -> READY on 2026-09-19 per the human's resolution of all five Blockers above (see this file's Blockers section and `docs/projects/active/session-runtime-v1/internal/LEGACY_HUMAN_REVIEW_2026-09-20.md`), then READY -> IMPLEMENTING the same day when implementation began.

**Implementation pass (2026-09-19)**: Implemented per this WORK's Approved Design as revised by the Blocker resolutions.

- `AnswerInteraction` is a new step directly on `sessionlifecycle.Manager` (`step_answer_interaction.go`), following the same Manager/narrow-repo-contract pattern as Create/Join/Leave/Start (Blocker 1). It carries no `session_requests` idempotency record - the interaction row's own persisted `state`/`response_payload` is the dedup identity; a retried equivalent response replays `Answered` (compared via decoded `engine.Value.Equal`, not raw bytes - see the real bug below), a conflicting different response is rejected as `Conflict`.
- The shared RuntimeTurn Step-draining/bound-execution mechanism was extracted from `step_start.go`'s former inline `drainRuntimeTurn` into a new package, `game/session/workflows/sessionlifecycle/internal/runtimeturn` (`Drain`, `MaxSteps = 20`, `Result` carrying `Err`/`FailedOnInitialSignal` so a caller can distinguish an ordinary rejection of the *initial* signal from any other failure) - scoped to the `sessionlifecycle` workflow tree, never at `game/session/internal` (Blocker 2). `step_start.go` now calls `runtimeturn.Drain`; its behavior is unchanged. Tests moved with it: `internal/runtimeturn/runtimeturn_test.go` (`TestDrain`'s five subtests, the same ones `step_start_test.go`'s former `TestDrainRuntimeTurn` had).
- A new shared capture mechanism, `interaction_capture.go` (`captureInteractions`, called by both `step_start.go` and `step_answer_interaction.go`), walks every drained Step's `Outputs` and persists each `engine.OpenQuestionOutput` as a new `ACTIVE` `session_interactions` row and each `engine.CloseQuestionOutput` as a Turn-produced closure of the matching `ACTIVE` row - closing the gap that no Turn-producing path (including Start's own first Turn) ever persisted `session_interactions` at all (Blocker 3). `session_interactions.kind` (`QUESTION`/`ASK_GROUP`) is resolved from the compiled `engine.Program`'s own `Workflows[...].QuestionSlots`/`AskGroupSlots` declarations, confirming Blocker 4's proposed approach is available exactly as designed against the actual compiler shape.
- **A real, previously-unverified engine behavior was discovered during implementation, not merely assumed**: answering a question does *not* itself produce a `CloseQuestionOutput` - `engineservice.Step` (`internal/runtime/step.go`) clears an accepted answer's own slot internally, before the transition's own authored operations run, so an authored `CloseQuestionOperation` on that same slot is by then an idempotent no-op producing no Output at all (verified directly against `execCloseQuestion`'s `pending == nil` early return). `CloseQuestionOutput` is therefore only ever produced by an authored `CloseQuestionOperation` closing a *different*, still-occupied slot. `AnswerInteraction` accordingly closes the one specific interaction it targeted directly, by its already-known id (`CloseAnsweredInteraction`, closing and setting `response_payload` in one write), rather than relying on `captureInteractions`' generic Output-scanning for that specific row; `captureInteractions` remains correct and necessary for every other case (Start's own opens, and any other authored close). This was caught by this WORK's own integration tests (the interaction row stayed `ACTIVE` after a successful answer) before being reported as done.
- **A second real bug found and fixed the same way**: comparing a stored `response_payload` (JSONB) against a freshly `engineservice.EncodeValue`-encoded answer via raw byte equality is unreliable, since Postgres's `jsonb` type does not preserve object key order across a round trip - the exact same class of defect already recorded elsewhere in this codebase (`getgame`'s known JSONB-comparison test defect). Fixed by decoding the stored payload back through `engineservice.DecodeValue` and comparing via `engine.Value`'s own `Equal` method instead of `bytes.Equal`.
- This slice's own fatal path (`terminalizeAnswerInteractionFatal`) atomically terminalizes the Session and closes every currently-`ACTIVE` `session_interactions` row for it (`CloseAllActiveInteractionsForSession`, `closed_by_turn_id NULL`, `closure_reason = SESSION_TERMINATED`) - the narrow terminal-cleanup case this WORK's own path needs, implemented now per Blocker 5's resolution; the fully general sweep (covering future entities like timers) remains Slice 6's.
- New migration `20260919000003_session_interactions.go` (registered in `migration.go`) creates `session_interactions` per the accepted shape, plus a partial unique index enforcing at-most-one-`ACTIVE`-row per `(session_id, engine_path, engine_slot, session_actor_id)` (mirroring the engine's own `ExecutionErrorSlotOccupied` invariant) and a partial index supporting the terminal-cleanup sweep. `engine_path`/`interaction_payload`/`response_payload` are `JSONB`, encoding `engine.Value`-typed data (a `[]engine.PathStep`'s `TaskKey`, an `OpenQuestionOutput`'s `Arguments` wrapped as a nameless `RecordValue`, and a submitted answer, respectively) through two new `engineservice.EncodeValue`/`DecodeValue` wrappers added to `engineservice/codec.go` (mirroring the existing `EncodeSnapshot`/`DecodeSnapshot` pattern exactly) - never plain `encoding/json`, which cannot correctly round-trip the `engine.Value` interface's variant types.
- `internal/repo/runtime_turn.go`'s `CreateRuntimeTurn` gained `sourceInteractionID *uint`/`actorID *uint` parameters (nil for Start, populated for AnswerInteraction's own caused Turn); a new `GetRuntimeTurn` read method loads a Turn's persisted `sequence`/`snapshot_payload` for RUNNING-phase resumption (GAME-ADR-0018's "reloads current committed `sessions.current_turn_id` and its corresponding authoritative Snapshot"). New `internal/repo/interaction.go`: `Interaction`, `ResolveSessionForInteraction` (unlocked pre-lock lookup, mirroring `ResolveSessionForJoinCode`), `FindInteractionByUUID`, `CreateInteraction`, `CloseActiveInteraction`, `CloseAnsweredInteraction`, `CloseAllActiveInteractionsForSession`.
- New domain vocabulary in `game/session/session.go`: `ErrInteractionNotFound`; `InteractionKindQuestion`/`InteractionKindAskGroup`; `InteractionStateActive`/`InteractionStateClosed`/`InteractionStateTerminated`; `InteractionClosureReasonSessionTerminated` (GAME-ADR-0019 leaves the exact enum naming an implementation-planning detail, not frozen by any ADR).
- Tests: `step_answer_interaction_test.go` (`TestManagerAnswerInteraction`'s pre-transaction `ResolveSessionForInteraction`/not-found check - the only part mockable without a real DB, mirroring `TestManagerJoin`'s/`TestManagerStart`'s own narrow scope); `step_answer_interaction_integration_test.go` (`TestManagerAnswerInteraction_Integration`'s 6 subtests: accepted answer committing a second Turn and resolving the interaction, rejection from a non-recipient, not-found for an unknown UUID, equivalent-response replay without re-execution, conflicting-response rejection, and the fatal path terminalizing the Session and closing its own active interaction); a new `step_start_integration_test.go` subtest (`first_turn_opening_a_question_persists_an_active_interaction`) proving the retrofit. `testutil_test.go` gained `answerableDefinition`/`answerableDefinitionWithFatalAnswer` fixtures (a real compilable/executable Definition whose root workflow opens a `Question` at `players[0]` immediately at Start and closes it on answer, the latter deterministically failing instead). `mocks_test.go` regenerated (`mockgen`) to add `MockanswerInteractionRepoAPI` and the updated `MockstartRepoAPI`.
- **Verification**: `go build ./...`, `go vet ./...`, `gofmt -l` (changed files) all clean; real Postgres brought up locally (`docker compose up -d postgres` with `DATABASE_USERNAME=playhoot`/`DATABASE_PASSWORD=DBPLAYHOOT`/`DATABASE_NAME=playhoot`/`DATABASE_PORT=5432`) and `go test ./... -count=1` run for real against it - every mocked-collaborator/pure-function test and every real-Postgres repository-integration/concurrency test passes (including all pre-existing Create/Join/Leave/Start tests, unaffected by the retrofit), with no new failure introduced beyond the already-recorded out-of-scope `getgame` JSONB-comparison test defect, exactly as this WORK's own Acceptance Criteria requires.
- Updated `game/CURRENT_STATE.md`, `game/docs/DATA_MODEL.md`, `game/docs/FLOWS.md` (new Answer Interaction section/sequence diagram, Start's sequence diagram updated to show `runtimeturn.Drain`/`captureInteractions`), and `game/docs/SESSION_RUNTIME_PERSISTENCE_MODEL.md`'s Not Yet Decided list (narrowed now that `session_interactions`' own enum values and `MAX_STEPS_PER_RUNTIME_TURN`'s constant surface are implemented) to describe the now-implemented behavior.
- **Deliberately flagged, not fixed here**: `activity_expires_at` (RUNNING inactivity expiration, `game/README.md`'s own accepted-but-unimplemented mechanism) has no schema or enforcement anywhere yet. AnswerInteraction is the first RUNNING-phase mutation this could plausibly apply to, but implementing it was never part of this WORK's approved Scope (silent about it, not requesting it) and Start never implemented it either - recorded in `game/CURRENT_STATE.md`'s Current Gaps as a pre-existing gap for a future slice to pick up, not a regression introduced here.

**WORK-0004's `Status:` was set to `IMPLEMENTING`** (transitioned from READY when implementation began) - it is not DONE: independent implementation review per `docs/ai/protocols/IMPLEMENTATION_REVIEW.md` remains outstanding, the one open gate every prior Slice's own checkpoint tracked before its eventual closure.

**Comment-standard cleanup pass (2026-09-19), same day**: per direct human feedback, source comments introduced by this implementation pass (and a handful of pre-existing ones found in the same sweep) referenced task/process state (`WORK-0004`, `Blocker N`, `Slice N`, "Approved Design") or narrated sibling implementations ("mirroring X's pattern") instead of describing the code's own contract/behavior, violating `docs/engineering/standards/code-comments.md`'s No Task/History Comments and No Artificial Coupling sections. Swept every `game/session/` Go file (production and test) for this pattern and rewrote each flagged comment to state the behavior/invariant directly, with no task/process references and no fragile cross-references to sibling code. `go build`/`go vet`/`go test ./game/session/... -count=1` re-verified clean after the sweep (comment-only changes; no behavior touched).

**Second comment-standard pass (2026-09-19), same day**: further human feedback identified that ADR/standards-doc citations (`GAME-ADR-0018`, `docs/engineering/standards/repositories.md`, etc.) are exactly the same problem in a different shape - a reader with no access to or knowledge of this repository's internal documents cannot use them, and a caller has no reason to know they exist. Revised `docs/engineering/standards/code-comments.md` (new "No Citing Internal Documents As A Stand-In For Explanation" section, plus an Enforcement bullet) to require every comment be self-contained in plain language, with no ADR/WORK/standards-doc citation as a stand-in for stating the actual reason. Re-swept every `game/session/` Go file (production and test, ~20 files) and rewrote each citation into the plain-language reasoning it stood in for, or removed it where the surrounding sentence already stood on its own. Re-verified clean (`go build`, `go vet`, `gofmt -l`, `go test ./game/session/... -count=1` against real Postgres) - comment-only changes, no behavior touched.

**Human code review (2026-09-19), same day**: the human reviewed `step_answer_interaction.go` directly and asked whether `interaction_capture.go`'s contents should move into it. Checked actual call sites first: `captureInteractions` and everything it calls are used by both `step_start.go` and `step_answer_interaction.go` - only `decodeEnginePath` is exclusive to answering. Flagged this to the human rather than moving the whole file; they chose to keep `interaction_capture.go` separate and move only `decodeEnginePath`. Applied that, then the human asked to revert it entirely - `encodeEnginePath`/`decodeEnginePath` stay paired in the same file even though `decodeEnginePath` has one caller, since that pairing matters more than the caller-count asymmetry. Reverted; `decodeEnginePath` is back in `interaction_capture.go` next to its encode counterpart. Re-verified clean after both changes.

**Independent review, round 1 (2026-09-19)**: Verdict **CHANGES_REQUIRED**, three REQUIRED_FIX findings, one NON_BLOCKING. (An earlier review attempt was launched with `isolation: "worktree"` and stopped immediately without producing a report, before it could review stale pre-commit code - worktree isolation checks out the last commit, which would have hidden this implementation entirely at that point; re-launched without isolation against the actual checkout, mirroring the lesson already recorded in WORK-0003's own history.) (1) **[HIGH]** No test exercises concurrent `AnswerInteraction` calls against the same interaction, despite the WORK's own Verification section and Acceptance Criteria explicitly requiring a concurrency proof mirroring Start's. Fixed: added `two_concurrent_answers_to_the_same_interaction_never_both_execute_a_runtime_turn`, proving the loser replays the winner's outcome via the same `sessionlock` serialization + equivalent-response-replay path already implemented, not a new mechanism. (2) **[MEDIUM-HIGH]** `captureInteractions`' `CloseQuestionOutput` branch was completely unexercised by any test - `answerableDefinition`'s own "Answered" transition closes the same slot being answered, which (per the verified engine behavior) never produces a `CloseQuestionOutput` at all, so no existing fixture ever triggered that branch. Fixed: added `interaction_capture_test.go` (`TestCaptureInteractions`, seven pure/mocked-collaborator subtests using a hand-rolled `fakeInteractionCaptureRepo`, mirroring `runtimeturn`'s `TestDrain` style) covering both Output kinds, both slot kinds, no-output skip, and two failure paths; and a new real end-to-end fixture, `dualQuestionDefinition` (two independent question slots; answering the first explicitly closes the second, still-pending one via `CloseQuestionOperation`), with a new integration subtest proving the generic capture path closes the *other* slot correctly in the same Turn. (3) **[MEDIUM]** Zero test coverage for the `AskGroup` interaction kind despite being explicitly in scope. Fixed: added `askGroupAnswerableDefinition` (an `OpenAskGroupOperation`/`AskGroupAllResponsesPolicy` fixture) and a new integration subtest proving `kind = ASK_GROUP` resolution, `SignalKindAskGroupAnswered` construction, and interaction closure all work end to end - writing this fixture required reading the engine's actual `stepAskGroupAnswer` implementation directly, which confirmed a fact the WORK's Out of Scope section had already anticipated: answering an ask-group member never selects a transition and never produces `InternalSignals`, so `AnswerInteraction`'s existing unconditional `CloseAnsweredInteraction` call (already written the same way for both interaction kinds) needed no changes at all for this to work correctly. (4) **[LOW, NON_BLOCKING, fixed anyway per human direction]** `captureInteractions` ran before `CloseAnsweredInteraction` in `answerInteractionInTx`, so an authored transition that reopens the exact same (session, path, slot, actor) key it just answered, within the same transition, would have hit the active-slot unique index before the old row closed. Swapped the call order - `CloseAnsweredInteraction` now runs first - removing the hazard at negligible cost.

Re-verified after fixes: `go build ./...`, `go vet ./...`, `gofmt -l` (changed files) clean; `go test ./... -count=1` against real Postgres - all `game/session/...` packages pass (including all nine `TestManagerAnswerInteraction_Integration` subtests and all seven `TestCaptureInteractions` subtests), no new failure beyond the same already-known out-of-scope `getgame` defect the reviewer independently confirmed.

**Independent review, round 2 (2026-09-19)**: Verdict **CHANGES_REQUIRED**, one REQUIRED_FIX (LOW). Round 2 independently re-verified every round-1 fix - re-read the reordered `step_answer_interaction.go`, re-ran the full suite (including the new tests individually, plus the new concurrency test under `-race -count=3`), and confirmed via `git diff` that no production file other than the intentional `step_answer_interaction.go` reorder changed - all four round-1 items are genuinely resolved, not just narrated. Its one finding: `game/docs/FLOWS.md`'s `AnswerInteraction` sequence diagram still showed the pre-reorder call order (`captureInteractions` before `CloseAnsweredInteraction`), stale relative to round 1's fix. Fixed: swapped the two diagram lines and updated the surrounding "Implemented behavior" bullet to state the ordering reason directly. While in the file, also removed several leftover `WORK-0004`/`Blocker N` references in `FLOWS.md`'s prose (missed by the earlier comment-standard sweep, which only covered `.go` files) - `game/CURRENT_STATE.md`/`DATA_MODEL.md`/`SESSION_RUNTIME_PERSISTENCE_MODEL.md`'s own `WORK-000N`/`Slice N` references were deliberately left alone, since those are this repo's established current-state/status-tracking convention (the same one `WORK-0001`/`WORK-0003` already use), not the caller-facing behavioral-comment problem the standard addresses. Re-verified `go build ./...` clean after the doc-only change.

**Independent review, round 3 (2026-09-19)**: Verdict **APPROVED**, no findings. Round 3 confirmed `FLOWS.md`'s diagram/prose now match the actual call order, confirmed no stray `WORK-0004`/`Blocker`/`GAME-ADR`/`Slice` reference remains in any `game/session/**/*.go` file, and re-verified `go build`/`go vet`/`gofmt -l`/`go test ./game/session/... -count=1` against real Postgres - all clean. It flagged one non-blocking documentation-drift note for the closing pass: `game/CURRENT_STATE.md` still said independent review had not yet run - synchronized as part of this closure (Session Runtime's row now says "independently reviewed (APPROVED)", the standalone Current Gaps bullet about the pending review was removed, and its `docs/work/active/...` evidence reference now points at `docs/work/completed/...`).

This WORK has no unresolved REQUIRED_FIX or DECISION_REQUIRED findings, no unapproved material deviation, and required documentation is synchronized to implemented reality. Feature Development completion criteria (`docs/ai/protocols/FEATURE_DEVELOPMENT.md`) are satisfied: Definition of Ready was met before implementation, implementation and tests are complete, verification ran (real Postgres, not merely the sandbox skip every earlier Slice recorded), independent review verdict is APPROVED, and required documentation synchronization is complete.
